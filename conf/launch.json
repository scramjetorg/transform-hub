{
    //ToDo: setup with watcher
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Check sample",
            "program": "${workspaceFolder}/src/samples/test/index.ts",
            "preLaunchTask": "tsc: build - src/samples/test/tsconfig.json",
            "outFiles": [
                "${workspaceFolder}/dist/samples/**/*.js"
            ]
        },
        {
            "type": "node",
            "request": "launch",
            "name": "Check current",
            "program": "${file}"
        }
    ]
}
