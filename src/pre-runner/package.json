{
  "name": "@scramjet/pre-runner",
  "version": "0.2.0",
  "description": "",
  "scripts": {
    "build": "docker build -t @scramjet/pre-runner # perhaps we could generate the name with a script?",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "AGPL-3.0"
}
