{
  "name": "scramjet-csi-dev",
  "version": "0.0.0",
  "description": "A development repo for Scramjet CSI, a container supervisor that allows deployment, execution and monitoring of any application based on a simple interface.",
  "main": "index.js",
  "bin": {
    "csi-supervisor": "bin/scramjet-csi-supervisor"
  },
  "scripts": {
    "build": "npm run build:supervisor",
    "build:supervisor": "tsc -p src/supervisor/tsconfig.json",
    "clean": "npm run clean:dist",
    "clean:dist": "find dist/ -mindepth 1 -not -wholename dist/.keep -delete",
    "postinstall": "find src/ scripts/ lib/ -name 'package.json' -not -wholename '*node_modules*' -execdir npm install \\;",
    "update": "",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-bdd": "cd bdd && ./node_modules/.bin/cucumber-js"
  },
  "author": "Scramjet <open-source@scramjet.org>",
  "license": "AGPL-3.0",
  "devDependencies": {}
}
