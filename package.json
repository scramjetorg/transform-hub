{
  "name": "scramjet-csi-dev",
  "version": "independent",
  "private": true,
  "description": "A development repo for Scramjet CSI, a container supervisor that allows deployment, execution and monitoring of any application based on a simple interface.",
  "main": "index.js",
  "bin": {
    "csi-supervisor": "bin/scramjet-csi-supervisor"
  },
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "pub": "lerna publish",
    "update": "",
    "lint": "eslint . --ext .ts",
    "watch": "echo \"Error: no watcher specified\" && exit 1",
    "postinstall": "lerna bootstrap",
    "test": "lerna run test",
    "test:parallel": "lerna run test --parallel",
    "test-bdd": "cd bdd && npm run test-bdd"
  },
  "author": "Scramjet <open-source@scramjet.org>",
  "license": "AGPL-3.0",
  "devDependencies": {
    "@types/node": "^14.14.25",
    "@typescript-eslint/eslint-plugin": "^4.14.0",
    "@typescript-eslint/parser": "^4.14.0",
    "eslint": "^7.18.0",
    "lerna": "^3.22.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ],
    "nohoist": []
  }
}
