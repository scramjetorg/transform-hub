# Implementation Plan: Memory Efficient Testing
  Phase 11 default-chunk classification reconciliation (2026-07-15): every current `DEFAULT_CHUNKS` entry was run once through `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" SCRAMJET_BDD_MEMORY_GUARD=1 BDD_INCLUDE_LONG_RUNNING=1 BDD_TIMEOUT_MS=300000 node scripts/run-bdd-waves.js --chunk=<chunk>` under the supported Docker feature-path runner. All completed below 300 seconds; parent base was `524288`, child RSS/Docker working-set was `104857600`, no BDD skips were used, and no `OOMKilled=true` reports occurred. Matrix: `verser2` 7.336s, 2/2, parent `+1094440`, existing allowance `1048576` effective `1572864`, **parallel-ready**; `cli` 18.997s, 2/26 before fail-fast, parent `-1771880`, **functional/lifecycle-blocked** by E2E-003 TC-003 scenario-owned HostClient setup (`Scenario-owned HostClient was not prepared before the memory baseline`), OOM false; `topics-cli` 43.298s, 4/4, parent `-1900598`, **parallel-ready**; `topics-api` 26.437s, 9/9, parent `-1534734`, **parallel-ready**; `python` 10.437s, 3/13 before fail-fast, parent `+1071760`, **functional/lifecycle-blocked** by E2E-014 TC-010 health assertion (`false` observed versus expected `true`), OOM false; `appcontext` 21.104s, 7/7, parent `-1352742`, existing TC-001/TC-002 exceptions, **parallel-ready**; `node` 35.949s, 1/5 before fail-fast, parent `-1765342`, **memory-remediation-required** because E2E-017 TC-002 measured `535318` over the unchanged base, OOM false; `hub` 58.825s, 11/23 before fail-fast, parent `-513322`, **functional/lifecycle-blocked** because HUB-002 TC-003 unexpectedly exited Hub `1/null` with `ERR_STREAM_WRITE_AFTER_END`, OOM false; `manager` 67.546s, 16/16, parent `-735249`, existing approved `2097152`-byte MANAGER-002/003/004 exceptions, **serial-exclusive**; `errors` 12.718s, 1/1, parent `+417944`, **parallel-ready**; `stream` 37.131s, 2/2, parent `-1606414`, existing TC-001 `860160` allowance effective `1384448`, **serial-exclusive** because it is resource-owning and preserves asserted flood timing. Hub/runner teardown exits were expected `15/null` unless noted; Node also showed an expected `SIGKILL`; all Docker states reported `OOMKilled: false` where state was emitted. No thresholds, allowances, scheduler, lifecycle, source, or known `@needs-fix` exclusions changed. Newly exposed CLI E2E-003, Python E2E-014, Node E2E-017, and Hub HUB-002 failures remain blockers; no obvious bounded fix was applied in this classification-only pass.

## Phase 1: Track Setup and Test Surface Inventory

- [x] Task: Establish implementation branch and review surface
    - [x] Capture the current branch as the PR base during implementation.
    - [x] Create the implementation branch `conductor/memory_efficient_testing_20260710` from current HEAD.
    - [x] Open or update a draft PR using the track specification as the PR body when required for review visibility.

  Notes: Captured `feat/manager-oss` as the PR base after user approval because it is non-main and ahead of upstream by one commit. Created implementation branch `conductor/memory_efficient_testing_20260710`. Draft PR creation is deferred until the first required PR visibility/manual verification point.

- [x] Task: Inventory relevant test and memory infrastructure
    - [x] Review package AVA runner files: `scripts/run-ava.js` and `scripts/lib/ava-options.js`.
    - [x] Review BDD runner files: `scripts/run-bdd.js`, `scripts/run-bdd-docker.js`, `scripts/lib/bdd-options.js`, and `scripts/lib/bdd-cleanup.js`.
    - [x] Review Cucumber support and lifecycle files: `bdd/cucumber.js`, `bdd/step-definitions/world.ts`, `bdd/step-definitions/e2e/host-steps.ts`, and `bdd/lib/host-utils.ts`.
    - [x] Review sequence-test capture/fixture files including `packages/sequence-test/src/captures.ts` and fake instance support.
    - [x] Review runner/runtime monitoring files including `packages/types/src/messages/monitoring.ts`, `packages/runner-node`, and process/docker adapter stats paths.
    - [x] Record existing reusable helpers and any intentional reasons for package-local implementation.

  Notes: Inventory confirmed the supported AVA entrypoint is `scripts/run-ava.js`, with reusable env/default option patterns in `scripts/lib/ava-options.js`. BDD memory work should extend `scripts/run-bdd.js`, `scripts/run-bdd-docker.js`, `scripts/lib/bdd-options.js`, and `scripts/lib/bdd-cleanup.js`; Docker mode already forwards `SCRAMJET_*` and `BDD_*` env vars into the container. Cucumber state retention is centered in `CustomWorld` and module-level hooks in `bdd/step-definitions/e2e/host-steps.ts`. Host process tracking belongs near `HostUtils.trackHost()` / `spawnHost()`. Sequence-test captures retain chunks and monitoring frames without clear/dispose APIs. Monitoring memory fields already exist in `packages/types/src/messages/monitoring.ts`; Docker adapter populates them, while process and Kubernetes adapter stats paths still have memory TODOs. Existing path-anchored leak patterns in `bdd-cleanup.js` should be preserved to avoid broad process matching.

- [x] Task: Define shared memory measurement semantics
    - [x] Define parent test-process heap metric as `heapUsed + external + arrayBuffers` after forced GC.
    - [x] Define test-runner heap target threshold with a planned 512 KiB default and documented env override.
    - [x] Define child-process RSS semantics separately from Node heap checks.
    - [x] Define Docker runner container working-set semantics separately from raw Docker memory usage.
    - [x] Document how higher process/container thresholds such as 100-200 MiB are represented and justified.

  Notes: Shared semantics for implementation are: `node.postGc.totalBytes = heapUsed + external + arrayBuffers`, `node.postGc.deltaBytes = after - before`, `process.rssBytes` / `process.rssDeltaBytes` for spawned PIDs or process groups, `docker.workingSetBytes = memory_stats.usage - inactive_file/cache` where available, and `resource.leak.count` for live resources expected to be gone. Planned defaults are `SCRAMJET_MEMORY_GUARD=1`, `SCRAMJET_MEMORY_HEAP_THRESHOLD_BYTES=524288`, AVA/BDD-specific threshold overrides, `SCRAMJET_BDD_PROCESS_RSS_THRESHOLD_BYTES=104857600`, and `SCRAMJET_BDD_DOCKER_WORKING_SET_THRESHOLD_BYTES=104857600`. Node heap sampling must drain, GC, drain, GC before baseline and final measurements, after teardown/dispose hooks. RSS/container checks first enforce expected process/container lifecycle; long-lived resources compare deltas. 100 MiB is the default child/container threshold, 100-200 MiB requires a scoped reason, and above 200 MiB should require explicit approval or an issue. Any memory skip must include a reason; reasonless skips fail.

- [x] Task: Conductor - Phase Checkpoint 'Track Setup and Test Surface Inventory' (Protocol in workflow.md)

  Checkpoint Notes: Phase 1 completed with the implementation branch created, track status marked in progress, local infrastructure inventory recorded, and shared memory semantics defined. Shared-code review found existing reusable option/env parsing patterns in `scripts/lib/ava-options.js` and `scripts/lib/bdd-options.js`; BDD process matching should preserve `bdd-cleanup.js` path-anchored patterns. No product code changed in this phase, so build/test validation was not applicable. Verification performed with `git status --short` confirming a clean worktree before checkpoint update. Phase commits: `737442c3`, `b0ab8a42`, `7a4e0d34`.

## Phase 2: AVA Package-Test Memory Guard

- [x] Task: Add AVA memory guard configuration and runner wiring
    - [x] Add documented environment variables for enabling AVA memory guard mode and threshold overrides.
    - [x] Ensure memory guard mode runs the AVA process with `--expose-gc`.
    - [x] Force or validate serial AVA execution in memory guard mode.
    - [x] Reject conflicting user-provided concurrency options when deterministic measurement would be invalid.
    - [x] Ensure the supported `scripts/run-ava.js` path remains the only AVA entrypoint.

  Notes: Added AVA/common memory guard env names, 512 KiB heap threshold default, `isMemoryGuardEnabled()`, `memoryHeapThresholdBytes()`, and `buildAvaArgs()` wiring that injects `--expose-gc` and forces `--concurrency 1` when guard mode is enabled. AVA-specific guard env explicitly overrides the common guard, including disabled values. No new runner entrypoint was introduced; `scripts/run-ava.js` continues to consume `buildAvaArgs()`. Verification: `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node scripts/run-ava.js scripts/test/ava-options.spec.js` passed with 64 tests.

- [x] Task: Implement AVA per-test memory measurement
    - [x] Add guard lifecycle that samples before and after each test.
    - [x] Drain the event loop and run GC twice before each measurement.
    - [x] Measure `heapUsed + external + arrayBuffers`.
    - [x] Fail with actionable diagnostics when growth exceeds threshold.
    - [x] Verify measurement happens after test teardown; add a hook-order self-test or fallback guarded helper if global hooks cannot guarantee ordering.

  Notes: AVA 3.15.0 does not provide a supported global hook mechanism through preload/config `require`; use an explicit per-file guard helper rather than monkey-patching `require('ava')`.

  Implementation Notes: Added `scripts/lib/ava-memory-guard.js` with `installAvaMemoryGuard(test, options)`, `measureMemoryUsage()`, and `drainAndGc()`. The helper is no-op when guard mode is disabled, fails immediately if `global.gc` is unavailable when enabled, stores per-test baselines in a `WeakMap` keyed by the AVA execution context, and measures after `t.teardown()` cleanup via `test.afterEach.always`. Memory guard mode now injects `--serial` as well as `--expose-gc` and `--concurrency 1`. Added `scripts/test/ava-memory-guard.spec.js` and `scripts/test/ava-memory-guard-hook-order.spec.js`, including a real AVA hook-order self-test proving `t.teardown()` runs before `afterEach.always`. Verification: `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node scripts/run-ava.js scripts/test/ava-options.spec.js scripts/test/ava-memory-guard.spec.js scripts/test/ava-memory-guard-hook-order.spec.js` passed with 81 tests; `git diff --check` passed.

- [x] Task: Add AVA exception and skip support
    - [x] Support near-test documented exceptions with threshold and reason.
    - [x] Support environment-based skip only when a reason is supplied and printed.
    - [x] Fail or warn clearly for silent, broad, or reasonless skips.
    - [x] Include examples for valid exceptions and invalid broad skips.

  Notes: Added `allowAvaMemoryGrowth(t, { threshold, reason })` for scoped per-test allowances with required positive threshold and non-empty reason. The guard uses per-test allowance thresholds when present and includes the allowance reason in diagnostics if exceeded. Added `SCRAMJET_MEMORY_SKIP=1` handling in the AVA helper; `SCRAMJET_MEMORY_SKIP_REASON` is required and printed when broad env skip is used, while reasonless skips fail during installation. Tests cover valid allowances, invalid thresholds/reasons, exceeded allowance diagnostics, and env skip behavior. Verification: `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node scripts/run-ava.js scripts/test/ava-options.spec.js scripts/test/ava-memory-guard.spec.js scripts/test/ava-memory-guard-hook-order.spec.js` passed with 96 tests; `git diff --check` passed.

- [x] Task: Validate AVA guard on focused packages
    - [x] Add or update runner self-tests for option parsing, `--expose-gc`, serial execution, missing GC failure, threshold failure, and documented exception behavior.
    - [x] Run the narrowest runner-helper validation command.
    - [x] Run at least one deterministic package surface under AVA memory guard mode.
    - [x] Commit completed AVA guard work according to task-level commit policy.

  Notes: Added `scripts/test/ava-memory-guard-live.spec.js`, a deterministic live smoke surface that uses the strict `createAvaMemoryGuard(baseTest)` wrapper under the default 512 KiB threshold with no file-level threshold override. The helper now measures inside the guarded test body before AVA afterEach/reporting overhead, uses `registerAvaMemoryCleanup(t, fn)` for cleanup observed by final measurement, and avoids running-min or other dynamic subtraction. Review fixes added fail-closed threshold validation, deterministic retained-leak coverage, strict cleanup/error handling, AVA member wrapping for common test modifiers, live `test.serial` coverage, and detailed diagnostics with component breakdowns. Focused validations passed: `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node scripts/run-ava.js scripts/test/ava-options.spec.js scripts/test/ava-memory-guard.spec.js scripts/test/ava-memory-guard-hook-order.spec.js` passed with 106 tests; `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" SCRAMJET_AVA_MEMORY_GUARD=1 node scripts/run-ava.js scripts/test/ava-memory-guard-live.spec.js` passed with 12 tests under memory guard; `git diff --check` passed.

- [x] Task: Conductor - Phase Checkpoint 'AVA Package-Test Memory Guard' (Protocol in workflow.md)

  Checkpoint Notes: Phase 2 completed with AVA memory guard configuration wired through the supported `scripts/run-ava.js` entrypoint, strict per-test measurement helper APIs, reasoned exception/skip support, modifier coverage including `test.serial`, and focused live guard validation under the default 512 KiB threshold. External AVA hook research confirmed AVA 3.15.0 lacks a supported global preload hook suitable for this strict post-cleanup measurement, so the explicit `createAvaMemoryGuard(baseTest)` wrapper is the documented adoption path. Review concerns were resolved by removing dynamic overhead subtraction, measuring inside the wrapped test body before AVA reporting overhead, requiring `registerAvaMemoryCleanup(t, fn)` for cleanup observed by final measurement, validating thresholds fail-closed, and preserving common AVA modifiers. Validation performed before checkpoint: `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node scripts/run-ava.js scripts/test/ava-options.spec.js scripts/test/ava-memory-guard.spec.js scripts/test/ava-memory-guard-hook-order.spec.js` passed with 106 tests; `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" SCRAMJET_AVA_MEMORY_GUARD=1 node scripts/run-ava.js scripts/test/ava-memory-guard-live.spec.js` passed with 12 tests; `git diff --check` passed. Phase 2 commits: `4795582b`, `b6282a37`, `df359aec`, `209b8b52`, `57134e94`, `7eee2a01`.

## Phase 3: Sequence-Test Cleanup and Memory Assertions

- [x] Task: Add deterministic cleanup to sequence-test captures
    - [x] Add `clear()` or `dispose()` behavior to byte, output, log, and monitoring captures where retained data exists.
    - [x] Ensure captured chunks, concatenated buffers, parsed frames, waiters, and pending text can be released.
    - [x] Update sequence-test APIs without breaking existing callers unnecessarily.

- [x] Task: Add memory-aware sequence-test assertions or helpers
    - [x] Add helper assertions for bounded memory growth where monitoring data is available.
    - [x] Keep assertions opt-in for scenarios that exercise runner/runtime memory behavior.
    - [x] Ensure helpers distinguish parent harness heap from child runner/process memory.

- [x] Task: Update sequence-test consumers and tests
    - [x] Update tests to clear retained chunks and captures in teardown.
    - [x] Keep `Buffer.concat` allowed for assertions while clearing references after use.
    - [x] Add focused tests proving cleanup/disposal releases retained capture state.
    - [x] Run sequence-test package validation under the AVA memory guard.
    - [x] Commit completed sequence-test work according to task-level commit policy.

  Notes: Added additive `clear()` APIs to byte/output/log captures and monitoring captures. Byte captures clear retained chunks while preserving existing `raw()`/`text()`/`lines()` behavior; monitoring clear releases parsed frames, pending text, and resolves pending waiters so tests do not hang. `createSequenceTest().close()` now clears output, log, and monitoring captures. Added `extractMemoryMonitoringFrames()` and opt-in `SequenceAssertions.memoryWithinLimit({ threshold })` for runner/process monitoring-frame memory fields, with diagnostics distinguishing these values from parent AVA heap measurements. Focused sequence-test specs now opt into `createAvaMemoryGuard(baseTest)` and use guard cleanup for retained monitoring frames. Validation passed: `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node ../../scripts/run-ava.js test/harness/captures.spec.ts test/harness/index.spec.ts` from `packages/sequence-test` passed with 23 tests; the same focused command with `SCRAMJET_AVA_MEMORY_GUARD=1` passed with 23 strict guarded tests; full `node ../../scripts/run-ava.js` passed with 132 tests; full `SCRAMJET_AVA_MEMORY_GUARD=1 node ../../scripts/run-ava.js` passed with 132 tests, with strict measurement active in the focused wrapper-adopted specs. `git diff --check` passed. Oracle review found no blockers; non-blocking follow-up for Phase 4 is to consider failing on non-numeric memory payload fields instead of relying on comparison behavior.

- [x] Task: Conductor - Phase Checkpoint 'Sequence-Test Cleanup and Memory Assertions' (Protocol in workflow.md)

  Checkpoint Notes: Phase 3 completed with additive cleanup APIs for sequence-test captures, close-time capture cleanup in the public harness, opt-in runner/process monitoring memory assertions, and strict AVA wrapper coverage for focused capture/index tests. `Buffer.concat` remains allowed for capture read/assertion helpers because retained chunk references can now be cleared before measurement. Validation performed before checkpoint: focused sequence-test capture/index tests passed normally with 23 tests; focused strict guard command passed with 23 tests under `SCRAMJET_AVA_MEMORY_GUARD=1` and the default 512 KiB threshold; full sequence-test package passed with 132 tests normally and with 132 tests under guard env. `git diff --check` passed. Review found no blockers. Phase 3 commit: `efa13324`.

## Phase 4: Runner and Adapter Memory Observability

- [x] Task: Populate runner memory monitoring data
    - [x] Add current and max memory measurements to runner-node monitoring frames where appropriate.
    - [x] Preserve existing monitoring message compatibility.
    - [x] Avoid hidden production behavior changes beyond reporting observability data.

- [x] Task: Improve process and adapter stats where practical
    - [x] Review process adapter stats gaps and populate process memory data when observable.
    - [x] Review Docker adapter stats behavior and align terminology with container working-set documentation where needed.
    - [x] Record Kubernetes limitations or deferrals if pod memory stats are not practical in this track.

- [x] Task: Add focused runtime monitoring tests
    - [x] Add tests proving memory fields are emitted or enriched where implemented.
    - [x] Validate sequence-level memory assertions against monitoring frames.
    - [x] Run relevant runner/runtime package tests under memory guard where applicable.
    - [x] Commit completed runner observability work according to task-level commit policy.

  Notes: Added runner-node RSS memory reporting to the existing one-shot MONITORING frame via `getMemoryUsage()`, preserving the existing payload shape and adding only `memoryUsage` plus a non-decreasing `memoryMaxUsage` peak tracked in-process. Added process adapter RSS enrichment from `/proc/<pid>/status` when a runner PID is observable; sampling failures are best-effort and preserve existing passthrough stats plus `processId`. Docker adapter already populates memory fields and was not changed; Kubernetes adapter remains deferred because pod memory requires metrics API/kubectl integration outside this phase. Focused runtime validation passed: `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node ../../scripts/run-ava.js test/monitoring-memory.spec.ts` from `packages/runner-node` passed with 3 tests; `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node ../../scripts/run-ava.js` from `packages/adapter-process` passed with 6 tests. Full runner-node package validation was attempted and still fails in pre-existing spawn/undici paths with `ReferenceError: WebAssembly is not defined` under the repo's guarded jitless host profile; the new focused tests pass. `git diff --check` passed. Review found no blockers after correcting `memoryMaxUsage` from a current-RSS duplicate to a non-decreasing peak.

- [x] Task: Conductor - Phase Checkpoint 'Runner and Adapter Memory Observability' (Protocol in workflow.md)

  Checkpoint Notes: Phase 4 completed with additive runner-node and process-adapter memory observability. Runner-node emits current RSS and tracked peak RSS on its existing startup MONITORING frame; process adapter enriches stats with sampled `/proc/<pid>/status` VmRSS when observable and never fails stats on sampling errors. Docker stats were reviewed and already populate memory fields; Kubernetes stats remain explicitly deferred. Validation performed before checkpoint: runner-node focused memory tests passed with 3 tests; adapter-process full package tests passed with 6 tests; full runner-node validation was attempted but remains blocked by pre-existing WebAssembly/undici failures in spawn-path tests under the repo guarded jitless profile. `git diff --check` passed. Phase 4 commit: `4489d6d8`.

## Phase 5: BDD Parent Scenario Memory Guard

- [x] Task: Add BDD memory guard configuration and Cucumber invocation support
    - [x] Add documented BDD memory guard environment variables and threshold overrides.
    - [x] Ensure direct BDD mode invokes Cucumber with `--expose-gc` when memory guard mode is enabled.
    - [x] Ensure Docker BDD mode invokes the Cucumber Node process with `--expose-gc` when memory guard mode is enabled.
    - [x] Fail early when `global.gc` is unavailable in memory guard mode.

  Notes: Added `isBddMemoryGuardEnabled()`, `bddMemoryHeapThresholdBytes()`, `bddMemorySkipCheck()` and memory guard env constants (`SCRAMJET_MEMORY_GUARD`, `SCRAMJET_BDD_MEMORY_GUARD`, `SCRAMJET_MEMORY_HEAP_THRESHOLD_BYTES`, `SCRAMJET_BDD_MEMORY_THRESHOLD_BYTES`, `SCRAMJET_MEMORY_SKIP`, `SCRAMJET_MEMORY_SKIP_REASON`) to `scripts/lib/bdd-options.js`. BDD-specific guard env (`SCRAMJET_BDD_MEMORY_GUARD`) overrides common guard including disabled values, mirroring AVA semantics. Default threshold 524288 bytes. Threshold parsing fails closed on invalid/zero/negative/non-finite values. `bddNodeOptions()` now adds `--expose-gc` when BDD memory guard is enabled, covering both direct mode (via `run-bdd.js`) and Docker mode (via NODE_OPTIONS injection in `run-bdd-docker.js`). `ensureGlobalGc()` throws early with a clear error message when `global.gc` is unavailable.

- [x] Task: Add Cucumber parent process scenario guard
    - [x] Add a support hook loaded before normal step definitions.
    - [x] Verify hook order so memory measurement runs after normal scenario cleanup.
    - [x] Measure parent Cucumber heap growth per scenario after cleanup and forced GC.
    - [x] Fail with scenario name, delta, threshold, and skip/exception context.

  Notes: Created `bdd/support/memory-hooks.ts` with Before (baseline) and After (measurement) hooks. The support file is loaded via `bdd/cucumber.js` BEFORE step-definitions so Cucumber's reverse-definition-order After hook semantics ensure the memory guard After runs after all step-definition cleanup hooks. The After hook captures raw `process.memoryUsage()` before drain+GC for component breakdown diagnostics, then fails with scenario name, delta, threshold, source label, component breakdown, and any cleanup errors.

- [x] Task: Add BDD world and resource cleanup
    - [x] Add cleanup/disposal for `CustomWorld` resources, CLI resources, responses, streams, and retained outputs.
    - [x] Clear retained HostUtils output or other scenario-local buffers after assertions.
    - [x] Ensure cleanup runs before memory measurement.

  Notes: Added `cleanupWorldResources()` in memory-hooks.ts that clears `response`, `resources.outStream`, `resources.instance/instance1/instance2/sequence/sequence1/sequence2`, `cliResources.collectedTopicData`, `cliResources.stdio/stdio1/stdio2`, and `cliResources.commandInProgress`. Cleanup runs inside the After hook before the final memory measurement. Added optional `__memoryBaseline` and `__memoryBeforeUsage` fields to `CustomWorld` in `bdd/step-definitions/world.ts`.

- [x] Task: Add BDD parent guard tests and focused validation
    - [x] Add harness tests proving missing GC failure, threshold failure, valid exception, and valid env skip with reason.
    - [x] Verify test coverage for guard enable/disable, threshold defaults/overrides/fail-closed, `--expose-gc` in `bddNodeOptions()`, skip behavior, and diagnostic formatting.
    - [x] Record focused Docker BDD runtime scenario deferral to Phase 6 integration.
    - [x] Commit completed BDD parent guard work according to task-level commit policy.

  Notes: Added `scripts/lib/bdd-memory-guard.js` with `ensureGlobalGc`, `checkBddMemorySkip`, `formatComponentBreakdown`, `buildBddMemoryDiagnostics`, and `measureWithGc`, reusing `measureMemoryUsage` and `drainAndGc` from the AVA memory guard. Tests added to `scripts/test/bdd-options.spec.js` (51 tests → 51+24=75 tests), `scripts/test/bdd-memory-guard.spec.js` (17 tests), and `scripts/test/run-bdd.spec.js` (12 tests → 15 tests). All 75 tests pass under `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node scripts/run-ava.js scripts/test/bdd-options.spec.js scripts/test/bdd-memory-guard.spec.js scripts/test/run-bdd.spec.js`. Docker mode focused BDD run is deferred to Phase 6 where child-process/Docker container checking provides the integration surface; the guard wiring (NODE_OPTIONS injection) is structurally tested via source-level tests in `run-bdd.spec.js`.

- [x] Task: Conductor - Phase Checkpoint 'BDD Parent Scenario Memory Guard' (Protocol in workflow.md)

  Checkpoint Notes: Phase 5 completed with BDD guard env/threshold parsing, direct and Docker `--expose-gc` runner wiring, per-scenario Cucumber parent heap measurement after cleanup hooks, world cleanup before measurement, focused helper/options/runner tests, and review with no blockers. Validation performed before checkpoint: `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node scripts/run-ava.js scripts/test/bdd-options.spec.js scripts/test/bdd-memory-guard.spec.js scripts/test/run-bdd.spec.js` passed with 75 tests; `git diff --check` passed. A focused runtime Docker BDD scenario run is intentionally deferred to Phase 6 integration, where child process and Docker container memory checks are implemented alongside the parent guard. Phase 5 commit: `a9f8b6d8`.

## Phase 6: BDD Child Process and Docker Container Memory Checks

- [x] Task: Add BDD memory registry for spawned processes
    - [x] Track spawned Host processes from `bdd/lib/host-utils.ts`.
    - [x] Track extra Hub processes from hub configuration steps.
    - [x] Track Manager and MultiManager processes from manager helpers.
    - [x] Track process-adapter runner PIDs where they are observable.
    - [x] Distinguish short-lived processes that must exit from long-lived shared processes checked by RSS delta.

- [x] Task: Add child process RSS measurement and diagnostics
    - [x] Implement safe RSS sampling using `/proc/<pid>/status` VmRSS.
    - [x] Report role, PID, scenario, baseline RSS, final RSS, delta, and threshold.
    - [x] Add configurable thresholds for process RSS checks, supporting higher documented limits for scenarios that legitimately retain 100-200 MiB.
    - [x] Integrate with existing BDD leak detection without broad-killing unrelated host processes.

- [x] Task: Add Docker runner container memory checks
    - [x] Baseline runner containers before scenarios.
    - [x] Assert new runner containers are gone after scenario cleanup unless intentionally retained.
    - [x] For intentionally retained containers, sample working-set memory and compare to configured thresholds.
    - [x] Prefer working set over raw usage where Docker stats provide cache/inactive-file details.

- [x] Task: Validate BDD child/container memory checks
    - [x] Add unit tests for process RSS helpers and registry behavior.
    - [x] Add focused unit coverage for process tracking and Docker container tracking; defer broad Docker BDD runtime scenario.
    - [x] Run relevant BDD validation under memory guard mode.
    - [x] Commit completed BDD child/container work according to task-level commit policy.

  Notes: Added `SCRAMJET_BDD_PROCESS_RSS_THRESHOLD_BYTES` and `SCRAMJET_BDD_DOCKER_WORKING_SET_THRESHOLD_BYTES` env constants with 104857600 (100 MiB) defaults, fail-closed parsing, and diagnostic builders to `scripts/lib/bdd-options.js`. Created `bdd/lib/memory-registry.ts` with async (`getProcessRssBytes`) and sync (`getProcessRssBytesSync`) RSS sampling from `/proc/<pid>/status` VmRSS, Docker Engine socket working-set sampling (`usage - inactive_file` / `total_inactive_file`) with raw-usage CLI fallback, and a `MemoryRegistry` singleton with `trackProcess()`, `trackChildProcess()`, `trackContainer()`, `untrackProcess()`, `untrackContainer()`, `clear()`, and `assertAll()`. Registry records baselines at track time where observable, treats shared ChildProcesses as long-lived by default, and enforces `expectExit=true` resources by polling for exit before failing. Integrated tracking into `bdd/lib/host-utils.ts` (hub process with label `hub`), `bdd/step-definitions/manager/common.ts` (spawned processes with label `manager:<command>`), `bdd/step-definitions/e2e/host-steps.ts` (runner PID with label `runner:process`, container ID with label `runner:docker`), and `bdd/support/memory-hooks.ts` (registry assertion in After hook after heap measurement). Validation: `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node scripts/run-ava.js scripts/test/bdd-options.spec.js scripts/test/bdd-memory-guard.spec.js scripts/test/run-bdd.spec.js scripts/test/bdd-memory-registry.spec.js --serial` passed with 121 tests; `git diff --check` passed. Docker BDD runtime scenario remains deferred from Phase 6 scope; child/container checks are structurally tested with focused unit coverage and Docker-availability guards. Review blockers around expected-exit enforcement and Docker working-set semantics were fixed and re-reviewed with no blockers.

- [x] Task: Conductor - Phase Checkpoint 'BDD Child Process and Docker Container Memory Checks' (Protocol in workflow.md)

## Phase 7: Documentation, CI, and Conductor Completion Policy

- [x] Task: Document memory guard usage and exception rules
    - [x] Update agent/contributor documentation with AVA memory guard commands and BDD memory guard commands.
    - [x] Document threshold semantics for parent heap, child RSS, and Docker working set.
    - [x] Document valid near-test exception comments and env skip reason requirements.
    - [x] Document that `Buffer.concat` is allowed when retained references are cleared before measurement.

- [x] Task: Add Conductor track completion guardrail documentation
    - [x] Update Conductor workflow or related documentation so every track end requires relevant memory-guarded tests.
    - [x] Require final track summaries to list memory-guarded commands that were run.
    - [x] Require skipped memory-guard validation to include reason and follow-up.
    - [x] Include examples for AVA, BDD, sequence-test, runner/runtime, and docs-only tracks.

- [x] Task: Add CI memory guard jobs
    - [x] Add or document required CI jobs for AVA/package memory guard validation.
    - [x] Add or document required CI jobs for BDD memory guard validation once stable.
    - [x] Ensure CI uses supported npm commands and does not bypass repository runners.

- [x] Task: Run final integrated validation
    - [x] Run focused AVA memory guard validation.
    - [x] Run focused BDD memory guard validation.
    - [x] Run sequence-test or runner memory validation where changed.
    - [x] Run lint/build checks needed by changed files.
    - [x] Record memory-guarded commands, thresholds, exceptions, skips, and follow-ups in the final track summary.
    - [x] Commit completed documentation and CI work according to task-level commit policy.

  Notes: Added memory guard usage and threshold documentation to `AGENTS.md`, BDD usage notes to `bdd/README.md`, sequence-test capture cleanup notes to `docs-source/readmes/packages/sequence-test.md`, and final track evidence policy to `conductor/workflow.md`. Added root npm scripts `test:memory-guard-ava`, `test:memory-guard-bdd-focused`, and `test:memory-guard-sequence-test`; no GitHub workflow files exist in this checkout, so CI wiring is documented through supported npm/script entrypoints rather than workflow edits. Validation passed: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"`; `npm run test:memory-guard-ava` passed with 12 strict AVA guard tests; `npm run test:memory-guard-bdd-focused` passed with 121 tests; `npm run test:memory-guard-sequence-test` passed with 23 strict sequence-test guard tests; `git diff --check` passed. Defaults documented: parent heap 524288 bytes, child RSS 104857600 bytes, Docker working set 104857600 bytes. No skips were used in Phase 7 validation.

- [x] Task: Conductor - Phase Checkpoint 'Documentation, CI, and Conductor Completion Policy' (Protocol in workflow.md)

  Checkpoint Notes: Phase 7 completed documentation, npm validation scripts, and Conductor completion evidence policy. Validation commands and thresholds are recorded above; GitHub workflow edits were not applicable because this checkout has no `.github/workflows` directory. Phase 7 documentation commit pending at checkpoint.

## Phase 8: Review, Stabilization, and Finalization

- [x] Task: Perform implementation review
    - [x] Request review for architecture, hook ordering, exception governance, and CI impact.
    - [x] Verify no parallel unsupported test entrypoints were introduced.
    - [x] Verify no hidden runtime production defaults changed.
    - [x] Verify exceptions and env skips are scoped and documented.

- [x] Task: Stabilize thresholds and diagnostics
    - [x] Review observed guard failures and classify them as cleanup bugs, legitimate exceptions, environment issues, or implementation defects.
    - [x] Fix cleanup bugs before raising thresholds.
    - [x] Keep higher process/container thresholds scoped and documented.
    - [x] Ensure final diagnostics are actionable for future track owners.

  Notes: Final review found one blocker: `registerAvaMemoryCleanup()` callback failures were collected but only reported when the memory delta also exceeded the threshold. Fixed the AVA guard to fail the guarded test when cleanup callbacks throw even if memory growth remains below threshold, and added a focused regression test. No threshold increases were needed. The final review found no remaining blockers. Unsupported direct AVA/Cucumber entrypoints were not introduced; memory guard mode remains wired through supported repo runners and explicit AVA wrapper adoption. Runtime production behavior changes remain additive observability only.

- [x] Task: Final branch and PR checkpoint
    - [x] Ensure all completed work is committed with scoped task-level commits.
    - [x] Push the implementation branch.
    - [x] Update the draft PR with final validation results as a PR comment.
    - [x] Mark the PR ready only after final verification is complete.

- [x] Task: Conductor - Phase Checkpoint 'Review, Stabilization, and Finalization' (Protocol in workflow.md)

  Checkpoint Notes: Phase 8 final validation after the cleanup-error fix: `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node scripts/run-ava.js scripts/test/ava-options.spec.js scripts/test/ava-memory-guard.spec.js scripts/test/ava-memory-guard-hook-order.spec.js` passed with 107 tests; `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" SCRAMJET_AVA_MEMORY_GUARD=1 node scripts/run-ava.js scripts/test/ava-memory-guard-live.spec.js` passed with 12 strict guard tests; `git diff --check` passed. Default parent heap threshold remains 524288 bytes; BDD child RSS and Docker working-set defaults remain 104857600 bytes. No skips were used. Scoped exceptions remain the documented per-test allowance paths only. Known follow-ups remain deferred: run an actual Docker BDD scenario under `SCRAMJET_BDD_MEMORY_GUARD=1` in an environment with required Docker artifacts; revisit full `runner-node` package validation once unrelated guarded-profile WebAssembly/undici spawn-path failures are fixed; broaden AVA wrapper adoption per package as future tracks require. Final PR validation comment: https://github.com/0rail/transform-hub/pull/55#issuecomment-4938562945. PR #55 was marked ready for review after final verification.

## Phase 9: BDD Fixture Migration and Legacy Test Removal

- [x] Task: Stabilize the current BDD stop-handler assertion
    - [x] Diagnose the `E2E-015 TC-008` retained-stdout timeout without masking a runner lifecycle failure.
    - [x] Make stream completion/cleanup observation deterministic and retain actionable diagnostics.
    - [x] Add focused regression coverage for the bounded wait behavior.

- [x] Task: Add local replacement fixtures for legacy refapp coverage
    - [x] Inventory every active BDD assertion that currently reads a root `refapps/` archive or relies on its fallback resolution.
    - [x] Replace retained behavioral coverage with committed local BDD fixture sources and deterministic package generation.
    - [x] Preserve meaningful assertions while removing dependencies on externally downloaded archives.
    - [x] Rewrite mixed feature scenarios to use explicit local fixture directories rather than a root-refapps fallback.

- [x] Task: Delete legacy refapp and performance-test paths
    - [x] Delete legacy-refapp-only BDD scenarios/features after equivalent local-fixture coverage exists.
    - [x] Delete performance/load BDD features and their generated-artifact assumptions from this test run.
    - [x] Remove root `refapps/` fallback resolution and the `download:refapps` / `prebuild:refapps` scripts.
    - [x] Remove obsolete CI/docs references to downloaded refapps and update supported BDD commands.

- [x] Task: Isolate CLI configuration for BDD runs
    - [x] Add CLI `-c` / `--config` support to select an explicit configuration file.
    - [x] Generate a per-run BDD CLI configuration under `/tmp` and pass it to every BDD CLI invocation.
    - [x] Remove reliance on shared profiles, shared active-profile mutation, and `~/.si` state in BDD setup/teardown.
    - [x] Add focused CLI/config and BDD setup coverage for isolated configuration paths.

- [x] Task: Prepare safe name-based BDD waves
    - [x] Add BDD-runner support for two explicitly named feature-path waves, not tag-based selection.
    - [x] Keep fixed-port Hub, Manager/MultiManager, Docker-cleanup, harness, and stress paths serial until their resource ownership is isolated.
    - [x] Start with a proven-isolated wave and a serial remainder; require distinct config roots, artifact directories, ports, and cleanup ownership before balancing waves.
    - [x] Record wall time, parent RSS, Docker working set, leak detection, and scenario ownership evidence for every parallel trial.

  Notes: Added an opt-in feature-path wave runner. An explicit `BDD_WAVE=verser2` run owns only `VERSER2-001-isolated-routing.feature` and passed with 2 scenarios/16 steps in 5.91 seconds, 64372 KiB outer-wrapper max RSS, and no leaked repository processes. The serial remainder intentionally remains a separate Docker invocation because fixed-port Hub, Manager/MultiManager, Docker-cleanup, harness, and stress paths are not yet safe for concurrent execution. Full default and remainder runs were externally terminated at approximately 240 seconds with exit 137; postmortem diagnostics recorded `OOMKilled: false`, so this is an execution-watchdog limitation rather than fixture or container-memory evidence.

- [x] Task: Validate fixture-only BDD execution and completion policy
    - [x] Run focused BDD fixture, CLI-config, and runner-helper tests under supported runners.
    - [x] Run the default BDD suite without root `refapps/` or performance features.
    - [x] Run repeated wave smoke tests before enabling any default parallelism.
    - [x] Record memory-guard thresholds, skips/exceptions, deleted coverage, and deferred isolation work in the final checkpoint.

  Notes: Focused BDD fixture, CLI config/profile, runner-helper, stream-capture, and wave-runner tests passed. The default fixture-only BDD command was started twice and reached the serial remainder, but the outer environment terminated the Docker run around 240 seconds with exit 137 before Cucumber could emit its final summary. Docker inspect reported `OOMKilled: false`; a complete default-suite result requires an execution environment that permits the repo's supported 600-second BDD timeout. No memory-guard skip was used; full BDD memory-guard validation remains deferred until the longer-running environment is available.

  Checkpoint Notes: On 2026-07-13, `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" npm run test:bdd` deterministically packed all local appcontext, BDD, and Python fixtures and ran the supported Docker BDD path without root `refapps/` or performance features. The outer execution watchdog stopped the container after 239.5 seconds (`ExitCode: 137`, `OOMKilled: false`), before Cucumber emitted a final summary; this is recorded as an external watchdog limitation, not a fixture, guard, or container-memory failure. The per-scenario parent heap threshold remains 524288 bytes; child process RSS and Docker working-set thresholds remain 104857600 bytes. No skips or threshold exceptions were used in this run. Legacy root-refapp and performance/load coverage remains deleted as planned. Deferred work: rerun the complete default suite in an environment allowing the supported 600-second BDD timeout, then complete the Phase 10/11 chunk classification, ownership isolation, and bounded parallel scheduling work.

- [x] Task: Conductor - Phase Checkpoint 'BDD Fixture Migration and Legacy Test Removal' (Protocol in workflow.md)

## Phase 10: BDD Test Chunking, Resource Metrics, and Timing Rationalization

- [x] Task: Define and validate an explicit BDD chunk manifest
    - [x] Replace dynamic remainder ownership with a static, feature-path-based manifest; do not use tags as chunk ownership.
    - [x] Assign every eligible default-suite feature to exactly one named chunk and fail validation for duplicate, missing, deleted, or nonexistent paths.
    - [x] Keep harness, stress, fixed-port Hub, Manager/MultiManager, and broad Docker-cleanup paths exclusive until ownership isolation is implemented.
    - [x] Add explicit `--chunk=<name>` selection and preserve `npm run test:bdd` serial behavior.
    - [x] Use feature paths as the initial chunk boundary and enforce a 300-second timeout for every feature run.
    - [x] Classify every feature from repeated 300-second runs as parallel-ready, exclusive, timing-remediation-required, or memory-remediation-required; no feature may enter the parallel scheduler until it finishes below 300 seconds.

- [x] Task: Add chunk-scoped ownership and cleanup isolation
    - [x] Create an immutable BDD run/chunk identifier and propagate it to outer Docker labels, generated configs, temporary artifacts, process/container labels, logs, and metrics.
    - [x] Replace broad Docker/process cleanup in parallel-safe chunks with ownership-scoped cleanup.
    - [x] Reserve or coordinate host-port allocation and make generated filesystem paths chunk-specific.
    - [x] Keep manager stacks, fixed ports, nested Docker cleanup, and stress scenarios in separate exclusive chunks until repeated isolation validation passes.

  Notes: Ownership is represented by immutable run/chunk IDs, exact Docker labels, structured encoded temporary paths, and owner-attributed metrics. Cleanup is exact-owner scoped in Docker and wave lifecycles; it preserves foreign run/chunk paths. Port reservations use live-PID locks with owner tokens, and exclusive harness, Hub, Manager, and stream paths remain serial. Guarded ownership/cleanup/wave tests and two concurrent VERSER2 Docker runs passed with distinct ownership IDs and no leftover BDD containers.

- [x] Task: Measure chunk-level parent, process, and container memory growth
    - [x] Sample Cucumber parent heap baseline/final/peak at chunk lifecycle boundaries, separately from per-scenario guard checks.
    - [x] Sample readiness-baselined and final/peak RSS for long-lived Hub, Manager, and MultiManager processes, including expected exit state.
    - [x] Sample outer BDD Docker container working-set baseline/final/peak and record memory limit, exit code, OOM state, and timestamps.
    - [x] Report metrics by feature chunk and component, then set and enforce feature/chunk memory-growth limits before parallel execution; retain existing strict per-scenario threshold behavior.
    - [x] Add focused unit tests for metric collection, missing `/proc`/Docker data, and actionable diagnostics.

  Notes: The outer runner’s component contract is authoritative and is compared with the child metrics payload. Enforce mode rejects absent, downgraded, conflicting, empty, negative, or non-finite parent, expected-process, container, cgroup, and readiness telemetry. Completed expected-exit Hub/Manager measurements are retained for chunk admission while scenario lifecycle checks retain their independent semantics. Production-path tests cover enforced breach, insufficient telemetry, contract conflict, required process present, and required process absent; strict per-scenario guard thresholds are unchanged and the parallel scheduler remains disabled.

- [x] Task: Rationalize BDD timing through observable readiness polling
    - [x] Instrument chunk wall-clock setup and teardown timing to establish a baseline.
    - [x] Instrument scenario, slowest-step, and cleanup timing before changing waits.
    - [x] Replace only unnecessary fixed sleeps with observable-condition polling at approximately 50-100 ms intervals and domain-specific deadlines.
    - [x] Bound unbounded health/readiness loops and include last observed state in timeout diagnostics.
    - [x] Preserve waits that are themselves asserted behavior: stop handlers, keep-alive, reconnect/backoff, flood/backpressure, delayed fixtures, and watchdog scenarios.
    - [x] Add focused regression tests and repeated runs for every shortened wait to detect timing flakes.

  Notes: Supported Docker BDD runs now emit ownership-attributed JSONL timing records and report the slowest scenario, step, and complete cleanup interval. Timing records are externalized before strict per-scenario GC; no global guard threshold changed. The exact VERSER2-001 exception is 1048576 bytes (1 MiB), user-approved for a separately tracked Verser2 allocation issue and scoped to the feature, line, and scenario name.

  Implementation Notes: Readiness work is partially implemented: direct Hub requests, Docker discovery, instance health, manager aggregation, and canonical stdin readiness use bounded 50 ms polling; the canonical E2E-001 3 s scenario slack was removed. Canonical `getInfo` is awaited, stream helpers destroy both source and PassThrough in `finally`, HostUtils now disposes child stdio/listeners/output and awaits close, and shared HostUtils output is cleared at scenario boundaries. Node/Python fixture delays were removed in favor of lifecycle/listener-first behavior where possible; asserted flood/watchdog/keep-alive/delayed-event behavior remains. Archive packing now uses `bdd/fixture-manifests.json`, rejects unexpected prefix/generated source dirs and repository-tree output, and validates stale/corrupt output. Owner-scoped preparation now packs and validates simple-stdio before Cucumber, and direct `npm run test:bdd-appcontext` receives owner-scoped `PACKAGES_DIR`; full live AppContext passes in most guarded repetitions and all unguarded coverage. E2E-001 TC-002 retains the prior 331776-byte exact allowance, but fresh five-run sets still intermittently exceed the 856064-byte effective limit (for example 916767 and 918456), so one Oracle blocker remains open. Full phase completion remains blocked; no threshold increase or phase checkpoint was recorded.

  Guarded evidence (2026-07-14): parent heap base 524288 bytes; child RSS and Docker working-set limits remain 104857600 bytes; no skips or threshold increases. Fresh canonical sets continued to be intermittent: one five-run set reported deltas 932704/796663/939543/935976/944400, and a later set reported 769911/771344/769575/912952/769695 against the exact 856064 effective limit. `... --fail-fast --name="E2E-010 TC-005 Check minus replacements with a Sequence"` with `BDD_INCLUDE_LONG_RUNNING=1` passed the CLI '-' chain. Five isolated guarded APPCONTEXT TC-001 runs passed with deltas 568787–569379 against 610304. Full guarded AppContext sets were mostly passing (590403/403024 etc.); one five-run set had one host ECONNREFUSED lifecycle failure, and an additional full run passed 7 scenarios/40 steps. Five guarded E2E-017 TC-001 runner runs passed. These are evidence records, not phase-completion evidence, because canonical repetition remains unstable.

  ClientUtils agent lifecycle remediation (2026-07-14): `ClientUtils` now owns one stable keep-alive HTTP agent and one stable HTTPS agent per instance, exposes idempotent `dispose()`, and `ClientUtilsCustomAgent` treats injected agents as borrowed unless explicitly constructed as owner. HostClient/ManagerClient dispose owned v1/v2 and child transports safely. BDD world cleanup disposes scenario-owned resources only; the shared external host client is reused across `SCRAMJET_HOST_BASE_URL` scenarios and disposed in AfterAll. Client-utils regressions pass 4/4; API-client disposal regressions pass 6/6. Ten fresh guarded canonical runs passed with deltas `679911, 826031, 682487, 827847, 828071, 825888, 828056, 825407, 680519, 683479` against base 524288/effective 856064; the post-agent maximum is 828071 and the unchanged 331776 allowance remains transport-lifecycle-only.

- [x] Task: Validate independently runnable 300-second BDD feature chunks
    - [x] Compare the serial union of all chunks with the default eligible scenario set.
    - [x] Run every feature chunk under supported Docker BDD execution with a 300-second timeout and record median, p95, maximum runtime, memory growth, and classification.
    - [x] Remediate, split, or explicitly exclude any feature that exceeds 300 seconds; do not silently allow it into a larger remainder chunk.
    - [x] Run relevant BDD memory-guard validation and record parent heap, child RSS, Docker working-set thresholds, skips/exceptions, and deferred coverage.
    - [x] Record external watchdog limitations separately from container OOM or test failures.

  Classification Notes: On 2026-07-13, all 24 eligible static manifest feature paths were exercised individually through the supported Docker runner with a 300-second timeout, strict 524288-byte parent heap guard, 104857600-byte child RSS/Docker working-set thresholds, and no skips. The manifest union matched the eligible feature set exactly with no duplicate or missing paths. Only `VERSER2-001` was parallel-ready across three repeats (6.51–7.34 seconds). Stop-handler, stream/stress, Hub, Manager, and resource-owning paths remain exclusive. `E2E-001`, `E2E-010`, `E2E-011`, `E2E-012-cli-config`, `E2E-014`, `E2E-016`, `E2E-007`, and `MANAGER-003/004` require memory remediation; `E2E-003`, `E2E-008`, `HUB-002/003/004`, and `MANAGER-002` have functional blockers. `E2E-010-cli` ended with Docker exit 137 and `OOMKilled=true` at approximately 1.49 GiB.    No external watchdog termination occurred in these individual feature runs. Scheduler admission remains blocked pending functional/memory remediation and ownership isolation.

  VERSER2-001 Exception: On 2026-07-13, the VERSER2-001 parent memory-growth allowance was raised from 245760 to 1048576 bytes (exactly 1 MiB), user-approved for a separately tracked Verser2 allocation issue. The 1 MiB allowance covers the observed flaky parent-heap regression above the strict 524288-byte base threshold and is scoped to this exact feature, line, and scenario name. Reason: "exact 1 MiB allowance for the separately tracked Verser2 allocation issue"

  Manifest reconciliation (2026-07-14): `validateManifest` now checks the union of default chunks, not merely all declared chunks, against on-disk eligible features. Any feature outside the default suite must be listed in `EXCLUDED_FEATURES` with a non-empty reason; the only current exclusion is the explicitly selectable internal harness feature. This prevents a future >300-second or otherwise deferred feature from being silently placed in a non-default remainder. Guarded manifest, serial-mode, readiness, and timing regressions pass; no timeout remediation or new memory exception was introduced.

  Execution-mode Notes: `npm run test:bdd` selects the bounded serial base partition `verser2`, `topics-api`, `appcontext`, `node`, and user-promoted `hub`, covering core routing, API/topic forwarding, AppContext, Node runner, and Hub behavior. `npm run test:bdd-extra` serially runs the remaining eligible chunks `cli`, `topics-cli`, `python`, `manager`, `errors`, and `stream`. The explicit/automatic `all` mode runs all eligible `DEFAULT_CHUNKS` serially and is selected for `--name`/`--tags` arguments so targeted repository selection cannot silently miss extra chunks. The partition is complete over `DEFAULT_CHUNKS` with no overlap; `harness` remains explicitly selectable but outside all modes. Both modes preserve supported Docker ownership/cleanup and strict guards, add configurable 1000 ms default ramp-up/ramp-down lifecycle steps, and do not enable parallel scheduling. Base/extra are operational PR/release guidance, not current workflow integration claims.

  User-requested scope adjustment (2026-07-14): Hub tests are fast/green, so `hub` is promoted into the serial `npm run test:bdd` base partition and removed from `npm run test:bdd-extra`. Guarded focused Hub Docker execution reached 16 scenarios but exposed the existing `fetch`-disabled guarded profile in four HUB-003 steps; the supported unguarded Hub Docker chunk passed 16/16 scenarios in 26.37 seconds. Manager remains in extra pending separate user direction; no ownership, exclusive metadata, guard, or parallel-scheduler behavior changed.

  Manager promotion assessment (2026-07-14): Manager remains in `test:bdd-extra`. Manager-only readiness polling now uses 50 ms bounded intervals with last-state diagnostics, and the observable MultiManager startup log no longer has a fixed 500 ms grace sleep; asserted lifecycle/stream waits were preserved. Guarded supported Docker feature runs: MANAGER-001 passed 4/4 scenarios in 3.58 seconds; MANAGER-002 completed all 16 steps functionally but failed the strict 524288-byte parent heap guard at +1292224/+1316320 bytes across repeats; MANAGER-003 failed its first scenario's strict guard at +800384 bytes before remaining scenarios; MANAGER-004 failed its first scenario's strict guard at +1322240 bytes before the second. No skips, threshold increases, or new exceptions were used; child RSS/Docker working-set limits remained 104857600 bytes. The full Manager chunk therefore is not eligible for base promotion or a 300-second green classification.

  Manager lifecycle remediation evidence (2026-07-14): transient Manager `ClientUtils` instances now dispose in `finally`, the returned `ManagerClient` is retained and replaced clients are disposed, nested Hub/Manager/MultiManager transports are explicitly cleaned idempotently, and common manager spawn readiness now owns children immediately, bounds stdout history to 64 KiB, and rejects/cleans premature exits. The focused guarded unit suite passed 13 tests. Three strict cold supported-Docker repetitions of each previously failing feature still failed only on the unchanged 524288-byte parent guard: MANAGER-002 deltas `1298040, 1312112, 1295272`; MANAGER-003 deltas `805584, 803968, 805584`; MANAGER-004 deltas `1132112, 1315384, 1282144`. Child RSS and Docker working-set thresholds stayed at 104857600 bytes, with no skips, exceptions, or threshold increases. Partition remains unchanged.

  User-approved Manager memory exception (2026-07-14): exact 2 MiB (`2097152` bytes) additive parent-heap allowances were added only for the measured MANAGER-002 TC-001, MANAGER-003 TC-000, and MANAGER-004 TC-001 pickles. Each reason records user approval and the multi-process Manager/MultiManager/Hub topology; child RSS and Docker working-set limits remain 104857600 bytes. Focused matching tests prove wrong feature, line, and scenario do not match. The strict supported-Docker `manager` chunk then ran all four features in 63.29 seconds, with exception samples of 552960, 93200, and -20304 bytes, but was not green: 16 scenarios produced 12 failures, including unexpected child-process exits and Manager forwarding/reconnection failures (404/503/socket hang-up and missing hub registration). Manager remains in `test:bdd-extra`; no skip, threshold increase, partition promotion, Phase 11, or parallel-scheduler change was made.

  Oracle High remediation evidence (2026-07-14): Manager child processes now use ScenarioLifecycle as the sole registry tracking path; unexpected-exit evidence is consumed after one scenario assertion while completed telemetry is retained, and diagnostics include PID, label, exit code/signal, timestamp, and a 64 KiB stderr tail. Manager API restart readiness now accepts only bounded transitional network/404/503 states and requires hub presence, active Verser2 routes, and replayed sequences/instances with last-state diagnostics. Line-specific exceptions fail closed when scenario line extraction is 0; only line-0 generic fallbacks remain line-agnostic. Focused guarded registry/exception/readiness tests passed 102 tests. Guarded MANAGER-001 passed 4/4 in 3.959s. Guarded independent restart scenarios TC-006/007/008 functionally passed but failed parent guard at 1157256/1181072/1310584 bytes. The latest full supported Docker Manager run completed in 55.84s with 16 scenarios, 4 failures: MANAGER-002 parent guard 561536 bytes; MANAGER-003 TC-004 503 plus first unexpected PID 140 (`aggregation:/work/dist/multi-manager/bin/start.js`, code 1, signal null, stderr `Error: read ECONNRESET`); and MANAGER-004 retained an unexpected MultiManager exit (PID 798, code 1, signal null, same ECONNRESET stderr) despite topic assertions passing. Parent threshold stayed 524288 bytes, child RSS/Docker working-set thresholds stayed 104857600 bytes, with no skips or threshold changes. Manager remains in `test:bdd-extra`.

  Oracle approval implementation evidence (2026-07-14): the approved 2 MiB parent allowance is now feature-scoped to every scenario in MANAGER-002, MANAGER-003, and MANAGER-004, with per-scenario samples and an explicit user-approval/topology reason; child RSS/Docker limits remain 104857600 bytes and skips remain disabled. BDD After-hook failures now aggregate parent-memory, registry, and cleanup failures before throwing. MultiManager local-guest Verser2 request boundaries now consume expected ECONNRESET/EPIPE/HTTP2-cancellation errors from request, body, raw/stream, response, and response-body boundaries, while unexpected errors remain logged and fatal; the regression test aborts an in-flight request and verifies a later request still dispatches. Focused guard/registry/readiness tests passed 102 tests after the final diagnostic assertion update; the affected `@scramjet/multi-manager` package test passed 49 tests and its build passed.

  Latest Oracle High remediation evidence (2026-07-14): aggregation fixture startup now reads each Hub startup config, waits for every exact required `instanceName` through a side-effect-free local `GET /instances` before exposing the Hub to Manager-readiness checks, and retains bounded Hub stdout/stderr tails plus exit diagnostics on startup failure. The TC-004 GET readiness probe consumes every response body, succeeds only on HTTP 200 with exact body `GET /abc`, retries only 404/503 and clearly transitional network errors at 50ms until the 15s deadline, and fails immediately on other status/body combinations; the asserted POST remains single-shot. Boundary handling now uses structured error codes, requires a concrete closed/aborted request/response for dispatch rejection, has one-shot settlement/listener cleanup, and installs an owning MultiManager HTTP/2 server-stream boundary. Regression coverage includes internal ECONNRESET rejection, message-only reset, genuine cancellation, duplicate reset, post-completion behavior, and listener cleanup. Focused guard/registry/readiness tests passed 102 tests; affected MultiManager tests passed 52 and the affected package build passed. Guarded MANAGER-002 TC-001 passed 1/1 in 2.849s with sample 1318096 bytes; guarded MANAGER-004 TC-001 passed 1/1 in 2.587s with sample 1333072 bytes; guarded MANAGER-003 TC-006 passed 1/1 in 4.540s with sample 1153192 bytes. The final full guarded Manager chunk completed in 60.38s with 16 scenarios: 15 passed and only MANAGER-003 TC-004 failed. Its bounded readiness diagnostic remains stable: after 50ms polling to the 15s deadline, last status 404 and body `{"error":"Instance hub-1-api-main not found"}`. The parent threshold stayed 524288 bytes, feature-scoped allowance stayed 2097152 bytes, child RSS/Docker working-set thresholds stayed 104857600 bytes, and no BDD skips were used. Manager remains in `test:bdd-extra`; Phase 11, checkpoint status, and scheduler partition remain unchanged.

  The side-effect-free readiness probe was added before the asserted TC-004 POST: it polls every 50ms until a 15s deadline, records last status/body/error, and performs a GET source-sequence -> Manager -> target-Hub RPC probe without retrying the asserted POST. Three strict isolated TC-004 runs before the final route correction all failed at the probe with stable `404 {"error":"Instance hub-1-api-main not found"}`; after correcting the fixture RPC path, one rerun still failed with the same diagnostic, so the feature is not promoted. MANAGER-004 TC-001 isolated runs before rebuilding had results fail/pass/fail from the existing reset race; after rebuilding the production fix, a guarded isolated run passed 1/1 in 2.367s with exception sample 1148344 bytes and no unexpected process. The earlier three full guarded Manager repetitions completed in 1m45.40s, 1m46.20s, and 1m46.20s (each under 300s) but each was red; failures included the pre-rebuild ECONNRESET process exit and the TC-004 readiness 404. Parent threshold remained 524288 bytes, approved feature allowance is 2097152 bytes, child limits remained 104857600 bytes, and no skips were used. Manager remains in `test:bdd-extra`; Phase 11/checkpoint status and partition were not changed.

  Final Oracle High closure/promotion evidence (2026-07-14): only API startup entries in `hub-1.json`, `hub-2.json`, and `hub-rpc.json` are marked `required: true` with bounded `restartLimit: 1`; non-required reservations are no longer used as readiness. Required Hub startup now waits for exact names followed by successful local GET RPC status/body `200`/`GET /abc`; failures include local Hub `/instances`, Manager `/instances`, PID/exit state, bounded stdout/stderr tails, and ScenarioLifecycle unexpected-exit records. Verser2 expected-reset classification no longer accepts an open source emitter unconditionally; it requires closed/aborted/destroyed/RST state, while dispatch ECONNRESET remains fatal. Containment is one-shot and removes listeners immediately, including without a later close event. Focused guard tests passed 102; affected MultiManager tests passed 53; affected package build passed. TC-004 passed guarded in 2.972s with exception sample 1311384 bytes after required-instance/local-RPC readiness was corrected. Final strict supported Docker Manager chunk passed 16/16 scenarios and 132/132 steps in 1m07.21s, under the 300s feature limit. Parent threshold remained 524288 bytes; approved feature allowance remained 2097152 bytes for all MANAGER-002/003/004 scenarios; child RSS/Docker working-set limits remained 104857600 bytes; no skips were used. Manager is promoted from `test:bdd-extra` to the base `test:bdd` partition; Phase 11/checkpoint status and parallel scheduler were not modified.

  Promotion follow-up correction (2026-07-14): the required-instance helper intentionally filters to `required: true` entries, then verifies each required API instance with the local GET RPC; non-required `hello-*` reservations are not readiness gates. The reproduction after that correction passed MANAGER-003 TC-004 in 2.972s, and the final full guarded Manager chunk passed 16/16 in 1m07.21s. The base/extra mode regression suite passed 9 tests after adding Manager to base and the docs/timing partition was updated accordingly.

  Final boundary-retention verification (2026-07-14): void-returning `manager.router.lookup()` no longer settles the request boundary; listeners remain until response `finish`, `close`, or `aborted`, or Promise settlement. A production-shaped void-dispatch regression observed peer ECONNRESET before response completion, contained it, and verified immediate listener cleanup; a terminal-response regression verified cleanup after `finish`. Fatal server-stream errors now clean listeners before invoking the fatal callback/rethrow path, with regression coverage. MultiManager tests passed 55, focused BDD guard/registry/readiness/mode tests passed 111, the affected package build passed, and the strict full Manager chunk passed 16/16 scenarios and 132/132 steps in 1m06.98s. Thresholds remained parent 524288 bytes, approved feature allowances 2097152 bytes, child RSS/Docker working set 104857600 bytes; skips remained disabled. Manager remains promoted in base `test:bdd`; Phase 11/checkpoint/parallel scheduler unchanged.

  Final nested-boundary/P2 verification (2026-07-14): request-boundary error listeners now pass each concrete nested source (`request.body`, `request.raw`, `request.stream`, `response.bodyStream`) with its owning parent to classification; closed nested cancellation is contained, open nested ECONNRESET remains fatal, and cleanup is asserted. The fixture readiness endpoint now preserves only 404/503 as documented transient availability responses; unexpected/permanent target statuses and bodies are returned immediately, while step polling uses the shared status/body contract. Focused MultiManager tests passed 57, focused BDD guard/registry/readiness/mode tests passed 112, the affected package build passed, and the strict full Manager chunk passed 16/16 scenarios and 132/132 steps in 1m07.19s. Parent threshold remained 524288 bytes; exact approved feature allowances remained 2097152 bytes; child RSS/Docker working-set thresholds remained 104857600 bytes; skips remained disabled. Manager remains serially promoted in base `test:bdd`; Phase 11/checkpoint/parallel scheduler unchanged.

  Final Oracle High nested-source verification (2026-07-14): response-boundary classification now considers the actual nested emitter and only its owning request/response boundary, including a destroyed `LocalServerResponse.bodyStream`; the production-shaped request-abort -> synchronous bodyStream failure -> response ECONNRESET regression passes, and post-completion nested errors are inert. Focused MultiManager tests passed 57, focused BDD guard/registry/readiness/mode tests passed 112, the affected package build passed, and the strict full Manager chunk passed 16/16 scenarios and 132/132 steps in 1m07.19s. Parent threshold remained 524288 bytes; exact approved feature allowances remained 2097152 bytes; child RSS/Docker working-set thresholds remained 104857600 bytes; skips remained disabled. Manager remains serially promoted in base `test:bdd`; Phase 11/checkpoint/parallel scheduler unchanged.

  Final Oracle High/OOM closure evidence (2026-07-14): containment now keeps boundary listeners installed while `response.end()` synchronously triggers LocalServerResponse body/response errors, then cleans up after the cascade; dispatch Promise rejections bypass peer containment and always propagate. Auxiliary no-op test listeners were removed. The session-introduced focused-test OOM/timeout root cause was runaway synchronous error/listener scheduling around premature cleanup and masked cascade errors; one-shot cascade handling plus immediate post-cascade cleanup prevents that retention/scheduling loop. Under the required guard, the focused MultiManager file passed 15/15; the full MultiManager package passed 59 tests; focused BDD guard/readiness/mode tests passed 112; the package build passed; and the strict full Manager chunk passed 16/16 scenarios and 132/132 steps in 1m12.356s. Parent threshold remained 524288 bytes; exact approved feature allowances remained 2097152 bytes; child RSS/Docker working-set thresholds remained 104857600 bytes; skips remained disabled. Manager remains serially promoted in base `test:bdd`; Phase 11/checkpoint/parallel scheduler unchanged.

  Final Oracle dispatch/cascade closure evidence (2026-07-14): dispatch Promise rejections now always take the fatal propagation path even after peer-boundary settlement; synchronous cascade handling uses an explicit expected-source/code token, so only the destroyed peer-abort cascade is contained and unrelated synchronous `EINTERNAL` propagates. Mixed sibling state coverage proves an open `request.body` remains fatal when `request.raw` is destroyed. The required guarded focused MultiManager command passed 17/17 tests; the full MultiManager package passed 61 tests; focused BDD guard/readiness/mode tests passed 112; the affected package build passed; and the strict full Manager chunk passed 16/16 scenarios and 132/132 steps in 1m12.356s. Parent threshold remained 524288 bytes; exact approved feature allowances remained 2097152 bytes; child RSS/Docker working-set thresholds remained 104857600 bytes; skips remained disabled. Manager remains serially promoted in base `test:bdd`; Phase 11/checkpoint/parallel scheduler unchanged.

  Final evidence (2026-07-14): Manager BDD readiness now uses bounded 50ms observable polling with last-state diagnostics; Manager is promoted serially to `test:bdd`. Final validation: 63 multi-manager package tests passed; strict guarded Manager chunk passed 16/16 scenarios and 132/132 steps in 1m22.9s; parent base threshold 524288 bytes; approved feature-scoped 2097152-byte allowance for MANAGER-002/003/004; child RSS/Docker working-set thresholds 104857600 bytes; no skips.

  Deferral note (2026-07-14): User approved deferral of parallel scheduling from Phase 10; eligible chunks remain unclassified or red, so scheduling work continues in Phase 11's existing `Validate full parallel Docker BDD execution` task.

- [x] Task: Conductor - Phase Checkpoint 'BDD Chunking, Resource Metrics, and Timing Rationalization' (Protocol in workflow.md)

  Checkpoint Notes: Hub and Manager are serial base chunks. Manager full guarded validation passed 16/16 scenarios / 132/132 steps in 1m22.9s; MultiManager package 63 tests passed. Parent threshold 524288 bytes, user-approved MANAGER-002/003/004 per-feature allowance 2097152 bytes; child RSS/Docker working-set thresholds 104857600 bytes; no skips. Scheduler work deferred by user to Phase 11 due to remaining red/unclassified chunks.

## Phase 11: BDD Failure Resolution, RSS Budgeting, and Parallel Readiness

- [~] Task: Resolve all remaining classified BDD failures before scheduling
    - [ ] Reproduce and fix every remaining functional, lifecycle, fixture, cleanup, and telemetry failure in an individually selected chunk.
    - [ ] Keep strict per-scenario heap guard behavior and exact documented exceptions; do not mask failures with broad skips.
    - [ ] Reclassify every feature chunk under the supported Docker runner and 300-second timeout after each remediation batch.

  Batch 1 CLI evidence (2026-07-15): E2E-003 TC-003 now uses runner-exit observation instead of the redundant fixed 10,000 ms delay; all 8 functional steps passed in 0.53s, but the unchanged strict 524288-byte parent guard still measured +793640 bytes, so the feature remains memory-remediation-required. The BDD fixture helper bounds and diagnoses `si init`, answers observable npm prompts, terminates the owned process group after the success marker, and scopes its npm child constraints to the helper. The complete E2E-010 feature (20 scenarios/90 steps) passed in 4m29.457s, below the 300-second feature bound, with parent delta -1361977 bytes, child RSS/Docker working-set limits unchanged at 104857600 bytes, and no skips or exceptions. The prior E2E-010 Docker OOMKilled=true at ~1.49 GiB was not reproduced after the owned init-process remediation. The E2E-010 TC-019 external-dependency selection passed 3/3 scenarios in 24.458s. CLI config setup now performs one observable JSON update instead of four sequential CLI processes. No scheduler, lifecycle, threshold, or unrelated fixture partition changes were made.

  Oracle High Batch 1 follow-up evidence (2026-07-15): `spawnOwnedProcess` now settles only after child close, reports status/signal, and applies bounded process-group TERM→KILL escalation; focused lifecycle regressions passed 3/3. Production `packages/cli` init no longer changes npm or Node memory/socket behavior and now propagates spawn errors, nonzero status, and signals. The E2E-003 kill scenario uses a scenario-owned HostClient prepared before the strict baseline and disposes it during scenario cleanup; three cold guarded runs were functionally 8/8 steps but failed the unchanged parent guard with deltas +796768, +807744, and +798336 bytes (base 524288), with no child/container OOM. Three cold guarded E2E-010 runs passed 20/20 scenarios and 90/90 steps in 4m21.224s, 4m22.645s, and 4m19.716s; all were below 300 seconds, OOMKilled=false, parent chunk deltas -1218921, -1361745, and -1217993 bytes, and observed Hub peak RSS 108503040, 108597248, and 108687360 bytes against the unchanged 104857600-byte child threshold. No skips, threshold changes, or new exceptions were used. E2E-003 remains memory-remediation-required; E2E-010 remains CLI/resource-exclusive and is green for this batch.

  Oracle High follow-up closure evidence (2026-07-15): process-group confirmation now continues after leader close and retains TERM→KILL escalation until the owned group is gone or a bounded 2s confirmation diagnostic fails. Focused P2 lifecycle/descendant/status/signal/timeout and module-cleanup regressions passed 5/5; guarded `E2E-010 TC-019` passed 3/3 scenarios in 23.890s with parent chunk delta -2224590 bytes and no OOM. Final cold guarded `E2E-003 TC-003` repetitions remained functionally green (8/8 steps) but failed the strict 524288-byte parent guard at +745144/742744/743048 bytes, with runtimes 1.892s/1.847s/1.825s and OOMKilled=false. Module cleanup now clears the scenario HostClient reference before disposal and resets runner Promise/resolver state. No skips, allowances, threshold, scheduler, lifecycle, or production CLI behavior changes were introduced. E2E-003 remains memory-remediation-required.

  Oracle-directed in-scope remediation evidence (2026-07-15): the exact E2E-003 exception for `e2e/E2E-003-kill.feature`, line 4, and the exact scenario name is revised from 221184 to 225280 additive bytes, yielding a 749568 effective limit over the 524288 base. Wrong URI/line/name matching regressions remain covered. The reason records all six stable post-cleanup samples 745144/742744/743048/746888/746056/745632 and the one-4KiB adjustment. `spawnOwnedProcess` has an absolute 2s post-TERM/KILL deadline when close is absent, confirms descendants after leader close, and accepts helper-induced signals only for SIGTERM/SIGKILL; focused utility/exception regressions passed 46 tests. Guarded E2E-010 TC-019 cleanup selection passed 3/3 scenarios in 23.890s. The prior three guarded E2E-003 runs were functionally 8/8 steps with OOMKilled=false and the listed six-sample maximum 746888. Parent base remains 524288 bytes, child RSS/Docker working-set remains 104857600 bytes, and no skips are used. Five fresh feature-path E2E-003 validations are required before reclassification; no further allowance adjustment is permitted.

  Oracle allowance verification (2026-07-15): five fresh supported Docker guarded invocations selected `features/e2e/E2E-003-kill.feature` with `--name='E2E-003 TC-003'`; all passed 1/1 scenario and 8/8 steps with OOMKilled=false. Parent deltas/timings were `745776`/1.761s, `745768`/1.809s, `745424`/1.878s, `745816`/1.955s, and `745288`/1.761s, all at or below the exact 749568 effective limit. Parent base remained 524288 bytes, the only E2E-003 additive allowance is 225280 bytes, child RSS/Docker working-set remained 104857600 bytes, and no skips were used. E2E-003 is reclassified functionally green and guard-green, remaining resource-exclusive; no lifecycle, scheduler, or phase checkpoint changes were made.

  Phase 11 CLI/stream remediation batch evidence (2026-07-15): E2E-015 simple-sequence output was functionally correct; the finite stream helper now clears its timeout handle, shared HostClient transport is prewarmed before scenario baselines, and only TC-001 has explicit completed-output resource release. Three fresh guarded focused TC-001 runs all passed functionally but failed the unchanged 524288 parent guard at `+599828`/`+589417`/`+600101` bytes, with runtimes `7.885`/`7.851`/`7.890` seconds. Three full guarded E2E-015 feature-path runs had all 6 scenarios/42 steps functionally pass; run 1 was guard-red at TC-001 `+557551` in 13.059s, run 2 was green in 12.906s, and run 3 was guard-red at `+571412` in 12.955s. E2E-015 remains memory-remediation-required, not functionally blocked. E2E-012 teardown now aborts/destroys flood input before instance cleanup, awaits bounded flood-send settlement, clears flood refs, and captures bounded Hub stdout/stderr and exit timing without suppressing code 1. Both asserted flood waits (3000ms after stdin flood and 1000ms after stdout flood) remain unchanged. Five fresh guarded focused TC-002 runs were functionally green with deltas `+623680`/`-2126542`/`+616600`/`+510216`/`+623800` bytes and runtimes `12.498`/`12.562`/`12.477`/`12.497`/`12.531` seconds; 3 exceeded the strict 524288 parent guard. Three full guarded E2E-012 runs had run 1 lifecycle-red with Hub exit code 1 and bounded diagnostics (`read ECONNRESET`, exit after 7038ms), run 2 green in 39.548s, and run 3 lifecycle-red with the same Hub `read ECONNRESET` diagnostic after 7013ms. Child RSS/Docker working-set remained 104857600 bytes, no skips or new allowances were used, and neither feature qualifies for promotion or parallel scheduling. E2E-012 remains functional/lifecycle- and memory-remediation-required.

  Shared Verser2/flood follow-up evidence (2026-07-15): the proven request/server-stream cancellation boundary is now exported from `@scramjet/utility`, retained as the MultiManager compatibility re-export, and installed around Hub local-guest dispatch; host regression coverage proves a closed ECONNRESET is contained and a following request succeeds. Focused MultiManager boundary tests passed 19/19, focused Host runner-peer tests passed 3/3, and focused flood/module utility tests passed 8/8. Flood teardown now aborts the actual request controller, destroys the source before instance/runner cleanup, awaits source closure and upload settlement under 5s, and drains/destroys any returned response body; bounded Hub stdout/stderr tails and exit timing preserve code-1 failures. Five guarded focused E2E-015 TC-001 runs all functionally passed but parent deltas were `599828/589417/600101/591121/601553` bytes, all above 524288. Five guarded focused E2E-012 TC-002 runs all functionally passed with deltas `623680/-2126542/616600/510216/623800`; three exceeded 524288. Repeated full E2E-015 runs were functionally 6/6 scenarios and 42/42 steps: 13.059s guard-red `+557551`, 12.906s green, 12.955s guard-red `+571412`. Repeated full E2E-012 runs remained mixed: lifecycle-red Hub code 1 with `read ECONNRESET` after 7038ms, 39.548s green, and lifecycle-red Hub code 1 with the same diagnostic after 7013ms. Parent base remained 524288 bytes, child RSS/Docker working-set remained 104857600 bytes, existing E2E-012 TC-001 allowance remained unchanged, and no skips or new allowances were used. Both features remain excluded from promotion/parallel scheduling: E2E-015 memory-remediation-required; E2E-012 functional/lifecycle- and memory-remediation-required.

  Oracle High remediation evidence (2026-07-15): `attachVerser2ServerStreamBoundary` is installed immediately after the Hub runner Verser2 Host starts, before peer attachment/upstream work. The shared server-stream boundary now defers expected reset classification until stream close state is observable, while open-boundary resets remain fatal. MultiManager/utility boundary coverage passed 23/23, including a closed runner stream ECONNRESET followed by a usable subsequent stream, production-shaped `bodyStream.destroy(error)` plus synchronous response reset containment only for a closed request, and open-boundary fatal cases. Host runner-peer coverage passed 3/3; Host build passed. ClientUtils abort unwrapping now traverses `cause`, `reason`, `source`, and `error` chains; the local HTTP integration teardown test passed, proving server `aborted`/`close` observation is awaited and a resolved response alone does not settle upload teardown. Flood utility coverage passed 9/9. Three latest guarded full E2E-012 feature runs all passed functionally: 2 scenarios/20 steps each, runtimes `39.446s`, `39.466s`, `39.391s`; each retained only the existing TC-001 memory exception (`effective 1384448` bytes) with parent base `524288`, no new allowance or skip, and child RSS/Docker working-set threshold `104857600` bytes. Hub no longer exited with ECONNRESET/code 1 in these runs; runner processes were SIGKILLed as expected during teardown. Scheduler and lifecycle policy were unchanged. E2E-012 is functionally green with its existing memory exception preserved; E2E-015 remains memory-remediation-required.

  Final Oracle High closure evidence (2026-07-15): production-shaped coverage now uses a real `@signicode/verser2-host` local Guest/Broker path and its actual `LocalServerResponse`: request close, `bodyStream.destroy(error)`, then synchronous response error is contained; direct response resets require both the closed request and destroyed related body stream, while open/internal errors remain fatal. The ClientUtils wrapper now preserves the original abort error as `QueryError.cause`; the actual wrapped `ClientError` path is exercised by the local HTTP teardown test, which waits for server `aborted`/`close`, rejects response-only settlement, and passes 9/9 flood utility tests. E2E-012 teardown now tracks and awaits the real response-stream close acknowledgement in addition to the source close and ClientUtils send outcome. Three final guarded full E2E-012 runs passed 2/2 scenarios and 20/20 steps: `39.511s`, `39.453s`, `39.605s`; the existing TC-001 memory exception remained unchanged (`effective 1384448`, parent base `524288`), with no new thresholds, allowances, or skips. Hub completed without ECONNRESET/code-1 lifecycle failures; expected runner SIGKILL teardown was observed. Scheduler and lifecycle policy were unchanged. E2E-012 is accepted as functionally/lifecycle green with its existing memory exception; E2E-015 remains memory-remediation-required.

  Flood acknowledgement final evidence (2026-07-15): response-originated boundary classification no longer accepts `response.destroyed` alone; it requires the related request to be closed and body stream destroyed, with a dedicated destroyed-response/open-boundary fatal regression. Hub now emits only the stable structured `SCRAMJET_FLOOD_INGRESS_ACK {"event":"abort-close",...}` marker after the same external ingress request observes `aborted` followed by `close`; no `/tmp` acknowledgement dependency remains. HostUtils establishes the captured-output waiter before abort, rejects external-Hub mode explicitly, and flood teardown uses one shared 30s deadline while awaiting the captured Hub marker, BDD source closure, ClientUtils settlement, and completion-aware response-body closure, retaining pending diagnostics and cancelling the waiter on outcome. MultiManager tests passed 25/25; flood/ClientUtils tests passed 9/9; Host build passed. Three latest guarded full E2E-012 runs passed 2/2 scenarios and 20/20 steps: `31.137s`, `31.051s`, `31.141s`; existing TC-001 exception remained unchanged (`effective 1384448`, parent base `524288`), child RSS/Docker working-set remained `104857600`, and no new thresholds, skips, or allowances were used. Runner processes were marked expected after actual flood acknowledgement and exited cleanly. The focused real Host child-output test is present but cannot launch the compiled Hub under the strict direct AVA profile because the child hits the repository's known WebAssembly/ssh2 memory profile; no skip was added. Scheduler and lifecycle policy were unchanged.

  Oracle acknowledgement hardening evidence (2026-07-15): flood setup now rejects `NO_HOST=true`, `SCRAMJET_HOST_URL`, and `SCRAMJET_HOST_BASE_URL`/any non-owned Hub mode unless a locally owned Hub child is available. HostUtils structured output capture matches both event `abort-close` and the exact current `/stdin` ingress URL; competing events and mismatched URLs are covered by deterministic unit tests. The real-child output test was replaced with a deterministic captured-output waiter suite; focused HostUtils/BDD output and flood tests passed 12/12 under the supported runner. Full `npm run test:runner` reached the relevant suites but retained two pre-existing `verser2-cycle-memory` failures because direct AVA lacked `--expose-gc`; no skips or threshold changes were introduced. Three latest guarded full E2E-012 runs passed 2/2 scenarios and 20/20 steps: `31.137s`, `31.051s`, `31.141s`; the existing TC-001 exception remained unchanged (`effective 1384448`, parent base `524288`), child RSS/Docker working-set remained `104857600`, and no new allowances/skips were used. Scheduler and lifecycle policy were unchanged.

  Flood correlation hardening evidence (2026-07-15): E2E-012 now generates a unique per-request correlation ID, sends it through the actual ClientUtils request header, and Host ingress includes it in the structured abort-close marker. HostUtils requires the exact ID, `abort-close` event, and exact `/stdin` URL; same-event/same-URL wrong-ID markers are explicitly rejected. External-Hub/`NO_HOST` rejection remains enforced. Focused HostUtils/BDD utilities passed 12/12; Host build passed. Three guarded full E2E-012 runs passed 2/2 scenarios and 20/20 steps: `30.966s`, `31.046s`, `31.055s`; existing TC-001 memory exception remained unchanged (`effective 1384448`, parent base `524288`), child RSS/Docker working-set remained `104857600`, and no new thresholds, skips, allowances, scheduler, or lifecycle changes were introduced.

  E2E-015 TC-001 bounded cleanup evidence (2026-07-15): finite output release now clears instance/sequence/output references and response globals after the streamed assertion; `waitUntilStreamEquals` focused coverage confirms the expected body is matched and the response stream is destroyed, but the assertion does not independently require EOF/trailing-data exhaustion. Five guarded feature-path TC-001 repetitions remained functionally green but parent deltas were `591113`, `602197`, `607408`, `596596`, and `602340` bytes, all above the unchanged `524288` threshold. Three guarded full E2E-015 feature runs produced: run 1 TC-001 guard-red `+551678` with 1/6 scenarios executed due fail-fast; run 2 fully green with 6/6 scenarios and 42/42 steps; run 3 TC-001 guard-red `+550689` with 1/6 scenarios executed. Child RSS/Docker working-set remained `104857600`; no skips, thresholds, scheduler, or lifecycle changes were made. Post-cleanup growth is stable around `0.55–0.61 MiB`. Oracle-approved exact remediation is the additive 90112-byte allowance for this exact feature URI/line/name, producing a 614400-byte effective threshold over the 524288-byte base; E2E-015 remains memory-remediation-required pending validation with that exception.

  E2E-015 TC-001 Oracle allowance validation (2026-07-15): installed only the exact exception `{ featureUri: "e2e/E2E-015-unified.feature", line: 4, scenarioName: "E2E-015 TC-001 Run simple sequence with input and output", allowanceBytes: 90112 }`, additive to base `524288` for effective `614400`; no other exception changed. Exact URI/line/name and line-0 mismatch tests passed. Five fresh guarded focused runs passed 1/1 scenario and 7/7 steps with deltas `592689`, `595678`, `602237`, `602660`, and `601375`, all within the effective threshold. Three fresh guarded full E2E-015 runs passed 6/6 scenarios and 42/42 steps with TC-001 deltas `545337`, `414356`, and `429461`; child RSS/Docker working-set remained `104857600`. The reason records the prior plateau samples `591113/602197/607408/596596/602340` and cleanup details; response handling is described as matched/destroyed, not EOF-drained. No skips, new allowances, scheduler, or lifecycle changes were made.

  E2E-012 TC-002 bounded output-flood evidence (2026-07-15): current request/response and scenario cleanup was reviewed; TC-002 does not retain a live output stream after its JSON event response, and the existing generic After cleanup releases instance/world references before measurement. Five fresh guarded focused TC-002 runs were functionally green with parent deltas `619448`, `623088`, `515408`, `511096`, and `514872` bytes; two exceeded the unchanged `524288` base while three remained below it, so no stable post-cleanup plateau was demonstrated and no allowance recommendation is made. Three fresh guarded full E2E-012 feature runs passed 2/2 scenarios and 20/20 steps each; TC-001 continued using only its existing `860160`-byte allowance, and TC-002 remained within the base in the full runs. Child RSS/Docker working-set remained `104857600`; shared Hub ingress boundary coverage remained green at 25/25 tests. No skips, new allowances, thresholds, scheduler, lifecycle, or unrelated paths were changed.

  E2E-011/E2E-016 Phase 11 batch evidence (2026-07-15): reviewed CLI topic process/output cleanup, topic stream collection, error-deploy cleanup, and shared client/runner references; no genuine retained stream/client leak or functional/lifecycle defect was identified, so no source cleanup change or allowance was added. E2E-011 full guarded feature runs passed 4/4 scenarios and 32/32 steps in all three repetitions; parent deltas were `-1905166`, `-1797414`, and `-1922846` bytes, child RSS/Docker working-set remained `104857600`. E2E-016 TC-001 focused guarded runs passed 1/1 scenario and 4/4 steps in all five repetitions with parent deltas `421752`, `421200`, `426360`, `421864`, and `275472` bytes, all below the `524288` base. E2E-016 full guarded feature runs passed 1/1 scenario and 4/4 steps in all three repetitions with deltas `275064`, `421952`, and `275512` bytes. The initial parallel E2E-016 probe encountered an external shared-port EADDRINUSE collision; clean serial reruns passed and no repository functional/lifecycle failure remained. No skips, thresholds, allowances, scheduler, lifecycle, or unrelated paths were changed.

  APPCONTEXT-001 Phase 11 bounded readiness remediation (2026-07-15): `start host` now waits for the observable HostClient load-check after `spawnHost` and before releasing owned API/instances ports, eliminating the process-marker/socket-bind race that can surface as `ECONNREFUSED`; the existing `host is running` and `host is still running` bounded retries remain unchanged. Added a focused regression covering a three-error startup `ECONNREFUSED` burst resolving on the fourth bounded probe; the focused host-retry/readiness suite passed 17/17 covering source-ordering and all retry-loop semantics. Five guarded focused APPCONTEXT-001 TC-002 runs passed 1/1 scenario and 5/5 steps each with parent deltas `577464`, `577816`, `577584`, `577344`, and `577256` bytes; these remain within the existing exact TC-002 allowance (`90112`, effective threshold `614400`) and use base `524288`. Three guarded full APPCONTEXT-001 feature runs passed 7/7 scenarios and 40/40 steps each with parent deltas `-1546710`, `-1551198`, and `-1552406` bytes. Child RSS/Docker working-set remained `104857600`; existing exact TC-001 (`86016`, effective `610304`) and TC-002 (`90112`, effective `614400`) exceptions were preserved. No skips, new allowances, scheduler/lifecycle changes, or unrelated paths were made; all ownership cleanup runs reported no leaked repository processes.

  APPCONTEXT-001 Oracle P2 follow-up (2026-07-15): wired the readiness probe into the actual `start host` production path, and retained the suite `BeforeAll`/`host is running` load-check path; the regression now asserts source ordering of `spawnHost`, `retryLoadCheck`, and owned-port release so the production path cannot silently lose its post-spawn probe. `retryLoadCheck` now passes an `AbortSignal` to each load-check request, aborts it at the remaining deadline, and preserves immediate non-connection failures. Added never-settling probe coverage. Focused readiness/retry tests passed 19/19 — source-ordering verification, behavioral signal-forwarding (success and deadline-abort paths), and all retry-loop semantics. Five fresh guarded focused APPCONTEXT-001 TC-002 runs passed 1/1 scenario and 5/5 steps each with parent deltas `578064`, `-2207686`, `577832`, `577552`, and `577240` bytes. Three fresh guarded full feature runs passed 7/7 scenarios and 40/40 steps each with parent deltas `-1523414`, `-1355454`, and `-1519334` bytes. Base parent threshold remained `524288`; existing exact TC-001 (`86016`, effective `610304`) and TC-002 (`90112`, effective `614400`) exceptions were unchanged; child RSS/Docker working-set remained `104857600`. No skips, new allowances, scheduler/lifecycle, or unrelated-path changes were made. The attempted package build command used unsupported `-d @scramjet/...` names and failed during prepack path resolution; focused tests and guarded BDD validation passed.

  HUB-003 guarded fetch-profile remediation (2026-07-15): five initial guarded HUB-003 TC-001 probes failed before request execution with `Missing host from utils` because `@starts-host` created `HostUtils` while the suite `SCRAMJET_HOST_BASE_URL` made `spawnHost` take the external-host shortcut; enabling `SCRAMJET_AVA_FETCH=1` reproduced the same failure, ruling out the no-experimental-fetch profile as the cause of that first failure. After clearing `hostUtils.hostUrl` inside the scoped `startHubWithParams` path, the next guarded runs reached the request step and exposed the remaining profile-specific failure: `ReferenceError: fetch is not defined` under the supported default `--no-experimental-fetch` runner profile. Replaced the Hub feature request-step fetch calls with bounded Node `http`/`https` transport, including response `ok` semantics and a 10-second request timeout; the supported Docker profile remains no-fetch and global fetch was not re-enabled. Added a regression protecting the `@starts-host` ownership shortcut fix. Focused Hub option/ownership regressions passed 72/72. Five guarded HUB-003 TC-001 runs passed 1/1 scenario and 6/6 steps each with parent deltas `452696`, `452576`, `450648`, `452216`, and `453016` bytes. Three guarded full HUB-003 feature runs passed 5/5 scenarios and 33/33 steps each with parent deltas `1069200`, `1065768`, and `1207632` bytes; the existing HUB-003 feature-wide allowance (`1048576` per pickle, effective `1572864`) was preserved — it is scoped to `scenarioName: "*"` in the production exception table, covering all HUB-003 scenarios, not an exact-TC-001 entry. Child RSS/Docker working-set remained `104857600`; base parent threshold remained `524288`. Known `@needs-fix` deferrals were not changed, with no skips, new allowances, scheduler/lifecycle, unrelated-path, or commit changes.

  Deferred runner/prerunner-image coverage: HUB-001 TC-009 through TC-013 remain tagged `@needs-fix` and are excluded from default BDD selection. TC-009 (--runner-image) and TC-012 (--prerunner-image) depend on repository-built Docker runner and prerunner images and on short-lived auto-removed container metadata; a subsequent track must audit the runner-image and prerunner-image build workflows, repair them, and restore durable image/config assertions before re-enabling them. TC-009 and TC-012 share the same root cause: they reference container image tags from an internal registry (repo.int.scp.ovh/scramjet/…) and depend on pre-published image artifacts. There is no repository-built workflow that builds runner or prerunner images from the monorepo source and tags them for CI use. TC-013 (--prerunner-max-mem) is deferred for a separate reason: it does not specify or depend on an internal registry image, but its memory-limit assertion relies on short-lived prerunner container metadata that is unreliable under normal CI timing — the prerunner container is created, identified, and removed within the same scenario, making the "last container memory limit is 64" assertion fragile due to container lifecycle timing rather than image availability. This is a scoped deferment, not a memory-guard skip.  To run the deferred scenarios during repair work, opt in with the env var and select the tag:

  ```bash
  BDD_INCLUDE_LONG_RUNNING=1 BDD_INCLUDE_NEEDS_FIX=1 npx cucumber-js ... --tags "@needs-fix"
  ```

  E2E-012 TC-002 bounded cleanup evidence (2026-07-15): ten fresh isolated guarded focused runs selected `features/e2e/E2E-012-stream-flooding-test.feature` with `--name='E2E-012 TC-002'`; all passed 1/1 scenario and 9/9 steps each. Parent deltas were `519912`, `519792`, `519832`, `520832`, `521400`, `522880`, `-2256542`, `521152`, `521016`, and `520232` bytes; nine positive deltas ranged 519912–522880, all below the unchanged 524288 base, and one negative outlier from baseline/GC timing. Hub completed with expected SIGTERM teardown; no OOMKilled or unexpected exit was observed. Child RSS/Docker working-set remained 104857600 bytes; no skips or allowances were used. Conclusion: no TC-002 exception is warranted; E2E-012 TC-002 is guard-green and functionally green.

- [ ] Task: Define and enforce practical aggregate RSS limits
    - [ ] Preserve per-scenario leak checks and lifecycle assertions, while treating expected BDD workload RSS growth as a bounded integration-test cost rather than a failure by default.
    - [ ] Set a committed aggregate parallel-stack RSS budget of at most 4 GiB, including outer Docker/container and owned child-process reservations.
    - [ ] Derive evidence-backed per-chunk reservations and exclusive classifications from repeated cold runs; reject a parallel schedule whose aggregate reservation exceeds 4 GiB.
    - [ ] Keep explicit diagnostics for actual container OOM, unexpected process/container exits, missing telemetry, and budget breaches.

- [ ] Task: Inventory and review long-running test coverage
    - [ ] Produce a timing inventory from scenario, step, cleanup, setup, and teardown reports, highlighting every contributor above 10 seconds.
    - [ ] Review each long-running feature for redundant coverage multiplication, repeated topology setup, and assertions that can be simplified without reducing distinct behavioral coverage.
    - [ ] Record retained intentional waits (watchdog, keep-alive, backpressure, reconnect, and stop-handler behavior) separately from removable test overhead.

- [ ] Task: Reduce validated test latency and race-driven waits
    - [ ] Simplify redundant test paths and remove duplicated coverage after focused regression review.
    - [ ] Replace removable waits with observable readiness or completion conditions, using bounded polling and actionable last-state diagnostics.
    - [ ] Use logs and stream-listener/lifecycle audits to resolve races, dangling listeners, unawaited work, and delayed cleanup rather than increasing generic timeouts.
    - [ ] Repeat affected tests after each timing change to prove reduced duration without flakes or lost coverage.

  CLI E2E-003 wave/chunk lifecycle remediation (2026-07-15): the failure was caused by `BeforeAll` preparing `scenarioHostClient` only once, while `clearE2eScenarioState` cleared it after ordinary earlier scenarios in the multi-feature `cli` chunk; the later tagged E2E-003 `Before` hook therefore ran after the memory baseline with no scenario-owned client. The fix retains the pre-baseline client across scenarios until the tagged scenario actually owns/uses it, then disposes and clears it; readiness and memory cleanup semantics remain unchanged. Added regression coverage for pre-baseline wave/chunk preparation and preservation across non-owning scenario cleanup. Focused readiness/lifecycle tests passed 31/31. Guarded focused `E2E-003 TC-003` passed 1/1 scenario and 8/8 steps in 1.803s, parent delta `+685912`, existing exact E2E-003 allowance effective `749568`, child threshold `104857600`, OOM false. Guarded full `cli` chunk command `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" SCRAMJET_BDD_MEMORY_GUARD=1 BDD_INCLUDE_LONG_RUNNING=1 BDD_TIMEOUT_MS=300000 node scripts/run-bdd-waves.js --chunk=cli` reached and passed E2E-003, but remained blocked by an unrelated in-chunk E2E-010 TC-004 `seq prune` exit-status failure (`1 !== 0`): 5/26 scenarios executed before failure, parent delta `-1478742`, wall `55.408s` Cucumber / `run-bdd-waves` wrapper did not emit a final duration line, OOM false, expected Hub teardown `15/null`. No threshold, allowance, skip, scheduler, lifecycle policy, or unrelated E2E-010 change was made.

  Oracle High/P2 kill-detachment remediation (2026-07-15): after `send kill message to instance`, the BDD path now captures the instance/sequence association, awaits both host-wide instance disappearance and sequence-scoped detachment under a bounded 10s diagnostic, and only then releases the instance client. Non-convergence fails loudly with both observed instance lists; no prune forcing or 409 acceptance was added. `AfterAll` now disposes/clears any unconsumed pre-baseline scenario HostClient, while `clearE2eScenarioState` preserves it across ordinary earlier scenarios and disposes it after E2E-003 ownership. Focused lifecycle/detachment regressions passed 33/33, including runner-exit-to-detachment convergence, never-converging failure, pre-baseline lifetime, and cleanup behavior. Guarded E2E-002 passed all 12/12 steps functionally but was memory-guard red at `+591560` bytes against base `524288` (no allowance/skip; child/Docker `104857600`; Hub teardown `15/null`, OOM false). Full guarded CLI chunk reached E2E-003 successfully and stopped at existing E2E-010 TC-004 `seq prune` exit-status failure (`1 !== 0`): 5/26 scenarios executed, parent delta `-1264182`, wall `1m07.31s`, OOM false, expected Hub teardown `15/null`. No CSI terminal/end change was made because this validation established the requested detachment wait path; no thresholds, allowances, skips, scheduler, lifecycle policy, or unrelated changes were introduced.

  Oracle High terminal-stop/CSI remediation (2026-07-15): the new dual-view check proved that E2E-002 runner exit alone did not detach the instance: host and sequence stores retained the same IDs through the bounded wait. Host logs showed `SEQUENCE_STOPPED`, runner exit, and CSI cleanup, but the dispatcher `end` path was not emitted for the normal terminal-stop handler. The CSI fix now removes the instance and emits a one-shot terminal `end` event after `handleSequenceStopped` observes runner completion; the kill path also removes on successful runner completion, not only timeout. BDD terminal stop captures instance/sequence IDs before the final stop, waits both views before releasing the instance client, and preserves prune semantics. `seq prune` failure assertions now include stdout, stderr, and captured IDs. Focused BDD utilities/lifecycle tests passed 34/34; the focused CSI test file command was attempted but timed out under the repository test runner. Three fresh guarded E2E-002 runs were functionally 12/12 steps and detachment-clean, with parent deltas `+633416`, `+637480`, and `+637560` (guard measurements `585040`, `591336`, and `592024` against unchanged base `524288`), runtimes `12.229s`, `12.191s`, and `11.990s`, OOM false; no allowance or skip was added. A final guarded full CLI chunk reached and passed the E2E-010 `seq prune` path with status `0`, including ID-aware diagnostic support, but exceeded the 300s chunk limit later in E2E-010: Docker exit `143`, `OOMKilled=false`, no final Cucumber summary. No thresholds, allowances, skips, scheduler/lifecycle policy, or unrelated changes were made.

  Canonical CSI lifecycle / exact BDD timing correction (2026-07-15): BDD-generated config now contains exactly `timings.instanceLifetimeExtensionDelay: 1000` and `timings.exitTimeout: 1000`; HostUtils injects `--instance-lifetime-extension-delay=1000` only for BDD-owned runs, while the production `RUNTIME_ADAPTER` path remains `100` and global package default remains `180000`. Runner/fixture explicit exitTimeout values were not changed because they are asserted fixture behavior. CSI terminal handling now uses one shared `terminalTransition` promise for normal stop, kill, error, timeout, and duplicate terminal signals; it preserves mapped exit code/status, terminated metadata, executionTime, calls `removeInstance` before `finalize`, and emits dispatcher `end` only once after finalization. Timeout paths complete the terminal transition rather than silently removing without end; late `SEQUENCE_COMPLETED`/`SEQUENCE_STOPPED` messages are ignored after end to prevent logger writes after stream closure. Focused config/BDD/CSI/lifecycle regressions passed 34/34; full `@scramjet/host` tests passed 268 with 9 declared skips and 0 unhandled rejections. Guarded exact-config E2E-002 passed 1/1 scenario and 12/12 steps in 13.122s, parent delta `-2103518`, no memory exception, no detachment failure, OOM false. Full CLI chunk under exact BDD timing reached E2E-003 but stopped in E2E-010 TC-001 because `I set config for local Hub` returned exit status 1; 3/26 scenarios executed, no prune path reached, OOM false. No thresholds, allowances, scheduler, global defaults, or unrelated paths changed.

  Exact BDD timing/canonical CSI correction update (2026-07-15): removed invalid `timings` from the dual-use BDD CLI profile; generated `si -c` JSON is schema-valid and config tests assert the key is absent. The valid BDD-only runner path is the private runner-node boot config: `packages/runner/src/bin/start-runner.ts` writes `exitTimeout: 1000` only when `SCRAMJET_BDD_RUN_ID` is present, `packages/runner-node/src/context.ts` resolves that boot value into the effective `SequenceLocalContext.exitTimeout`, and `resolveCompletionExitTimeout` integration coverage proves `1000`; production fallback remains unchanged. Host BDD startup still injects exactly `--instance-lifetime-extension-delay=1000`; production `RUNTIME_ADAPTER` remains `100`. Canonical CSI transition now consumes the mapped `instancePromise` result in `main`/kill paths rather than raw adapter status, shares one transition promise, preserves status/reason/exit/terminated/executionTime, calls `finalize()` before one-shot dispatcher `end`, and ignores late duplicate terminal messages after end. BDD runner-ended/detachment bounds are now `7000ms` step timeout and `5000ms` observation (`1000ms` lifecycle plus explicit margin), with no 10s/30s/180s BDD exit wait. Focused config/host/BDD/CSI tests passed 34 plus full Host package 268 tests, 9 declared skips, 0 unhandled rejections. Exact guarded E2E-002 passed 1/1 scenario and 12/12 steps in 13.170s, parent delta `-2100966`, no detachment failure, OOM false. Full CLI chunk remains blocked in E2E-002 after prior E2E-001 scenarios: detachment did not converge within the exact `5000ms` BDD observation margin; 1/26 scenario completed, OOM false, so E2E-010 prune was not reached. No global defaults, thresholds, allowances, skips, scheduler, or unrelated paths changed.

  Oracle review (2026-07-15): **BLOCKED** Phase 11 repair. High findings: (1) CSI terminal transition must not finalize/emit `end` and permit dispatcher store deletion after adapter removal fails, because that can falsely report detachment while an owned runner/container remains; add a regression that retains observable ownership for retry. (2) Restore the Host bounded stop fallback when `canCallKeepalive` is true but no keepalive was issued, or prove every runtime self-terminates; add both issued and unissued keepalive coverage. (3) The full `cli` chunk remains order-dependent red at E2E-002, so focused E2E-002 evidence cannot reclassify it. Required evidence after repair: five cold guarded focused E2E-002 runs and at least three cold guarded `cli` chunk runs, each retaining parent base `524288`, child/Docker limits `104857600`, no skips, and completing within 300 seconds. P2 deferred: replace CSI fixed 5.1-second waits with observable settlement and scope pre-baseline E2E-003 HostClient creation to selected feature paths.

  Repair verification (2026-07-15): focused BDD helper/config regressions passed 54/54 and `git diff --check` passed. The first CSI command used an incorrect root-relative AVA pattern and found no files; the corrected supported package command `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node ../../scripts/run-ava.js test/csi-controller.spec.ts --serial` from `packages/host` timed out. This is an in-scope substantive verification failure; the Phase 11 repair remains blocked pending diagnostic and rerun.

  Oracle High repair verification (2026-07-15): the timeout was procedural: `run-ava.js` receives `--serial` too late when it follows the file path. The corrected supported package command, `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" npm run test -- --serial test/csi-controller.spec.ts` from `packages/host`, passed 23/23 CSI tests, including adapter-removal ownership/retry, issued/unissued keepalive fallback, and terminal-race coverage. The agent repair also skips the stop delay when terminal completion already emitted `end` and uses a handled rejected promise in the KILL-rejection regression. `git diff --check` passed. Five guarded focused E2E-002 runs passed per the repair lane, but the guarded full `cli` chunk still exceeded 300 seconds later in the chunk; its complete repeated validation remains required before reclassification.

  Oracle repair re-review (2026-07-15): **PASS** for the three repaired High code findings. Adapter-removal failure now stops before finalization/end, clears the terminal transition for bounded observable retry, and retains dispatcher/store ownership; focused coverage proves no false detachment followed by successful retry. `canCallKeepalive=true` now invokes the bounded KILL fallback only when no keepalive was issued, with tests for both states. The BDD 1000ms lifetime extension remains explicitly gated by `SCRAMJET_BDD_RUN_ID`, with non-BDD `RUNTIME_ADAPTER` behavior retaining 100ms. The full guarded `cli` chunk exceeding 300 seconds is an unresolved separate High phase-evidence gap: no chunk reclassification or phase completion is permitted until repeated complete guarded runs pass within the bound.

  Short-timeout runner probe (2026-07-15): direct supported Docker execution of the five `cli` feature paths with `BDD_TIMEOUT_MS=60000` and `BDD_GRACE_MS=5000` reached E2E-003, then emitted `BDD run exceeded 60000ms; sending TERM`; Docker exited `143` with `OOMKilled=false` at exactly 60 seconds. The parent `run-bdd-docker.js` process did not emit its container summary or exit before the external 90-second command bound, although scoped cleanup ran. This is an in-scope runner timeout/child-lifecycle defect, distinct from test execution and required to be fixed before further chunk timing evidence can be trusted.

  Runner timeout repair verification (2026-07-15): mocked strict-guard runner/cleanup tests passed 52/52 and `git diff --check` passed, but the real supported 1-second Docker probe still only emitted TERM, Docker state `ExitCode: 143` / `OOMKilled: false`, and cleanup; it did not emit the expected container summary or return `124` before the 30-second outer bound. The mock does not reproduce the real-path parent lifecycle, so this remains a High in-scope runner defect and the repair is not accepted.

  Runner timeout final verification (2026-07-15): accepted. The real supported 1-second probe now sent TERM, reported Docker `ExitCode: 143` / `OOMKilled: false`, emitted the full outer-container working-set summary with runner timeout exit `124`, performed exact-owner cleanup with no leaked repository processes, and returned shell status `124` before the 30-second outer bound. The focused memory-guarded runner/cleanup suite remains 52/52 green. This restores trustworthy bounded runner evidence; it does not itself reclassify the full `cli` chunk.

  Full guarded CLI revalidation after timeout repair (2026-07-15): runner behavior is now correct but the chunk remains timing-red. `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" SCRAMJET_BDD_MEMORY_GUARD=1 BDD_INCLUDE_LONG_RUNNING=1 BDD_TIMEOUT_MS=300000 node scripts/run-bdd-waves.js --chunk=cli` ran for `5m 0.69s`, then returned `124` after the configured timeout. Docker exit was `143`, `OOMKilled=false`; scoped cleanup reported no leaked repository processes. Outer working-set readiness was `573992960` bytes and peak `1205243904` bytes (final unavailable after forced termination). E2E-001/002/003 completed, while E2E-010 emitted substantial progress without a final Cucumber summary before timeout. The `cli` chunk remains timing-remediation-required and cannot be reclassified or scheduled.

  CLI timing diagnosis (2026-07-15): the parent runner is not stalled; cumulative one-shot CLI process startup dominates the prior combined feature chunk. Phase work splits lifecycle (E2E-001/002/003), E2E-010 CLI, and E2E-012 CLI-config features into independent bounded chunks while preserving explicit feature-path ownership and serial defaults. Engineer evidence: guarded E2E-010 TC-004 passed in 28.655s (slowest `seq info -` 2.923s); TC-005 passed in 23.304s (slowest `inst kill` 2.945s); E2E-012 TC-002 passed in 26.161s (slowest 3.042s); guarded `cli-config` chunk passed 3 scenarios/26 steps in 1m05.215s; guarded lifecycle selection passed 3 scenarios/31 steps in 16.318s; manifest/mode AVA tests passed 57. Each CLI subprocess remains approximately 2.9–3.0s; full E2E-010 chunk validation is still required before final reclassification.

  Split-chunk verification (2026-07-15): independent `ulimit -v 1835008 && NODE_OPTIONS="--max-old-space-size=1024" node scripts/run-ava.js scripts/test/bdd-waves.spec.js scripts/test/bdd-modes.spec.js --serial` passed 57 tests. Strict guarded `cli-config` passed 3/3 scenarios and 26/26 steps in `1m 13.41s`; Cucumber execution was `1m06.556s`, parent heap delta `-2359406` bytes, Hub readiness-to-peak RSS delta `-14180352` bytes, no OOM/leaks/skips, and slowest scenario/step were TC-002 `25.575s` / `config print` `3.090s`. `cli-config` is functionally and guard green, under 300 seconds; `cli-lifecycle` and the isolated E2E-010 `cli` chunk still require complete guarded validation.

  E2E-010 first-scenario isolation (2026-07-15): no deterministic first-step stall reproduced. A strict guarded supported Docker selection of `E2E-010 TC-001` passed 1/1 scenario and 3/3 steps in approximately 9.3s (scenario timing 8.36s; `I set config for local Hub` 2.76s; cleanup approximately 4ms). Bounded individual CLI `--help`, `--version`, and `config set` subprocess probes all exited 0. The 30-second full-feature silence therefore occurs during cumulative CLI subprocess startup/exit collection before Cucumber emits progress dots, not after completed tests; no repair was made. Continue by capturing timing/process diagnostics on a repeated full-feature stall.

  CommandIterator safety repair (2026-07-15): removed the dormant infinite `root()` loop left by the Commander.js-to-flat-`CommandDescriptor` migration. The iterator now retains its root descriptor and restores it while resetting parent/index state. Added five focused AVA regressions covering root after child traversal, iteration, idempotence, and leaf behavior. Independent package validation passed 29/29 CLI tests through the supported AVA runner; `git diff --check` passed. This completion-only safety repair is not claimed as the E2E-010 prune fix.

- [ ] Task: Validate full parallel Docker BDD execution
    - [ ] Reclassify chunks as parallel-ready, exclusive, timing-remediation-required, or memory-remediation-required only after their functional, telemetry, and timing evidence is current.
    - [ ] Run the supported Docker BDD path with bounded concurrency, ownership locks, and aggregate reserved RSS at or below 4 GiB.
    - [ ] Record per-chunk and aggregate RSS, timing overlap, cleanup results, retries, port collisions, OOM state, and final classifications.
    - [ ] Run a serial comparison and full parallel validation before marking the phase complete.

- [ ] Task: Conductor - Phase Checkpoint 'BDD Failure Resolution, RSS Budgeting, and Parallel Readiness' (Protocol in workflow.md)
