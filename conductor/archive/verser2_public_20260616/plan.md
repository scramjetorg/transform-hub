# Implementation Plan: Clean verser2 GitHub token override from public surface

## Phase 1: Public Surface Cleanup and Validation

- [x] Task: Audit public-facing token guidance
    - [x] Search docs, README files, examples, package metadata, generated docs sources, CLI-visible text, and scripts for verser2-specific GitHub Packages token guidance.
    - [x] Classify each match as public-facing, internal maintainer/agent-only, historical Conductor context, or unrelated dependency metadata.
    - [x] Record the audit findings and cleanup targets in the track notes.
    - Audit summary: public surfaces had no verser2-specific token setup docs; `.env.example` exposed a generic `GITHUB_PACKAGES_TOKEN` placeholder that could imply required setup. Active internal install/check paths requiring token injection were `.npmrc`, `scripts/check-verser2-github-packages.js`, `scripts/build-all.js`, `packages/runner-python/scripts/install-deps.sh`, and the lockfile's `@signicode` GitHub Packages resolved URLs. Remaining historical references are in Conductor rollout/archive files or unrelated `@scramjet` GitHub Packages docs.
- [x] Task: Confirm helper ownership and install behavior
    - [x] Locate the reusable GitHub Packages helper/recovery path.
    - [x] Confirm which scripts or package install paths still rely on the helper while verser2 remains on GitHub Packages.
    - [x] Identify wording that should become generic rather than verser2-specific.
    - Result: the previous helper was the verser2-specific checker script. It was converted into an unauthenticated public npmjs package-resolution check. Generic private GitHub Packages recovery guidance remains in `conductor/known-solutions.md` for future private packages.
- [x] Task: Remove verser2 install authentication requirement
    - [x] Identify active verser2 package checks, install scripts, npm config, lockfile metadata, and CI/helper code paths that require or inject `GITHUB_PACKAGES_TOKEN`, `NODE_AUTH_TOKEN`, or equivalent auth for verser2 packages.
    - [x] Update verser2-specific package checks/install paths to use public npmjs access without requiring token injection.
    - [x] Keep the reusable GitHub Packages authentication helper available for future private packages, but decouple it from the active verser2 public package path.
    - [x] Verify unauthenticated/public verser2 package access with the narrowest package check or install command available.
    - Result: removed the root `@signicode` GitHub Packages registry/token override, removed token forwarding from `scripts/build-all.js`, removed `GITHUB_PACKAGES_TOKEN` fallback from the Python install script, updated the checker to use public npmjs, and refreshed active `@signicode` dependencies/lockfile to public npmjs `0.4.2` after user approval.
- [x] Task: Clean public documentation and messaging
    - [x] Update public docs, README content, examples, or surfaced script messages to avoid telling users to configure GitHub tokens specifically for verser2.
    - [x] Keep any required install messaging generic and private-package-oriented where appropriate.
    - [x] Ensure no tokens, secret examples, or private credential values are introduced.
    - Result: `.env.example` no longer suggests `GITHUB_PACKAGES_TOKEN`; script output now references public npmjs resolution, not GitHub Packages package-read tokens.
- [x] Task: Preserve internal recovery notes
    - [x] Update internal Conductor/known-solution notes only if needed so maintainers can still recover authenticated GitHub Packages installs.
    - [x] Keep internal notes clearly scoped as private maintainer/agent recovery information, not public verser2 setup documentation.
    - [x] Preserve the reusable helper for future private package needs.
    - Result: `conductor/known-solutions.md` now documents a generic private GitHub Packages recovery pattern with temporary `.npmrc` userconfig and no verser2-specific setup guidance.
- [ ] Task: Validate cleanup and no behavior drift
    - [x] Rerun targeted searches for verser2 GitHub token override wording across public surfaces.
    - [x] Verify remaining matches are internal-only, historical, or unrelated and document the rationale.
    - [x] Run `npm run check:verser2-packages` without token override if install/auth-related scripts or package resolution guidance changed.
    - [x] Run `npm run lint` or targeted lint only if source/script files changed.
    - [x] Run `npm run build:packages` only if package/source behavior or generated package output changes.
    - Search summary: active package metadata and lockfile now reference `0.4.2` from `registry.npmjs.org`; stale `0.4.1`/GitHub Packages matches are historical Conductor archive/rollout notes or unrelated non-`@signicode` package versions.
    - Validation: `npm run check:verser2-packages` passed without token overrides; script syntax checks (`node -c` for JS scripts and `bash -n` for runner-python installer) passed; `npm install --package-lock-only --ignore-scripts --dry-run` passed without token overrides; `npm run lint` passed; `npm run build:packages` passed and completed the `dist/` workspace install without GitHub Packages auth.
- [x] Task: Finalize track artifacts
    - [x] Update the track plan with validation results, skipped checks, and rationale.
    - [ ] Update PR/body or handoff notes if this track is attached to an active PR.
    - [x] Commit scoped changes after validation passes.
- [x] Task: Conductor - User Manual Verification 'Public Surface Cleanup and Validation' (Protocol in workflow.md)
    - Approved by user after validation passed.
