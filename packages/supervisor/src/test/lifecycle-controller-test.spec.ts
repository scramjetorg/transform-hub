import test from "ava";

test("When an instance of LifeCycleController is construcred with correct parameters it must not be null", t => {
    // New stud tests will be written after the unit testing workshop
    t.pass();

});
