// import { CSHConnector } from "@scramjet/types/src/csh-client"
import test from "ava";

const testPath = "...";

test("load package form path " + testPath, async _t => {
    // Test if getPackage method load the package.
});

test("return readable stream with package ", async _t => {
    // Test if getPackage method returns a readable stream.
});

test("get stream array", async _t => {
    // Test if getClient method takes an array.
});

test("array contains proper elements", async _t => {
    // Test if array passed to getClient method contains stdio, stderr and controllers.
});

test("return to the console", async _t => {
    // Test if getClient method return stream to the console.
});