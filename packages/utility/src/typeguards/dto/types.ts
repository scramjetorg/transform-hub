/**
 * Local DTO type stubs for utility package.
 *
 * Simplifications of types previously imported from the old types package.
 * These are used in runtime type guard functions, so strict type shape
 * is not critical at compile time.
 */
export type StartSequenceDTO = any;
export type StartSequenceEndpointPayloadDTO = any;
export type SetSequenceEndpointPayloadDTO = any;
