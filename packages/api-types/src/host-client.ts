/**
 * Stub for HostClient API client interface.
 *
 * Phase 2 stub structurally compatible with the full HostClient from
 * the old shared types package via the index signature, without importing old types.
 * Full REST API client type contract will be defined in Phase 3.
 */
export interface HostClient {
    // Phase 2 stub — index signature for structural compat with old
    // HostClient. Full type in Phase 3.
    [key: string]: any;
}
