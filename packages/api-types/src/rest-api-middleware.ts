/**
 * MWRestAPI namespace — Middleware REST API DTO types.
 *
 * Minimal structural stubs for the types used by middleware-api-client.
 * Full DTO definitions remain in @scramjet/types.
 */
export namespace MWRestAPI {
    export type GetMultiManagerResponse = any;
    export type GetMultiManagersResponse = any;
    export type GetVersionResponse = any;
}
