
  ✖ split runner sends PING before any later monitoring or terminal frame first monitoring frame must be PING
  ─

  split runner sends PING before any later monitoring or terminal frame

  test/transport/split-runner-communication-ordering.spec.ts:113

   112:     t.true(monitoringFrames.length > 0, "at least one monitoring frame …
   113:     t.is(firstFrame?.[0], RunnerMessageCode.PING, "first monitoring fra…
   114:     t.is(firstFrame?.[1].sequenceInfo?.id, INSTANCE_ID, "PING sequenceI…

  first monitoring frame must be PING

  Difference:

  - 3012
  + 3000

  › test/transport/split-runner-communication-ordering.spec.ts:113:7

  ─

  1 test failed
