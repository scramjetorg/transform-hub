import { VerserClientOptions } from "../types";

/**
 * Default VerserClient configuration.
 */
export const defaultVerserClientOptions: VerserClientOptions = {
    headers: {},
    verserUrl: "http://localhost:8080"
};
