{
  "name": "@scramjet/example",
  "version": "0.7.0-pre.1",
  "main": "index",
  "types": "dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "build": "tsc -p tsconfig.json && cp data.json dist/",
    "clean": "rimraf -rf ./dist",
    "watch": "tsc -b --watch",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "prepack": "LOCAL_PACKAGES=true node ../../../scripts/publish.js",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=10",
    "scramjet": ">=0.9"
  },
  "scramjet": {
    "image": "node"
  },
  "author": "Signicode <open-source@signicode.com>",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/node": "14.14.33",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "4.1.4"
  },
  "dependencies": {
    "JSONStream": "^1.3.5",
    "scramjet": "^4.35.15"
  },
  "publishConfig": {
    "registry": "https://repo.int.scp.ovh/repository/npm-scramjet/",
    "access": "restricted"
  }
}
