#!/usr/bin/env ts-node

import sequence from "../index";
const { PassThrough } = require("stream");


// here I call the function from index.ts out

sequence(new PassThrough(), "./data.json", "./dataOut.txt");
