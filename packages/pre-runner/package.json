{
  "name": "@scramjet/pre-runner",
  "version": "0.2.0",
  "description": "",
  "scripts": {
    "build:docker": "docker build -t scramjet/pre-runner .",
    "start": "./analyze.sh",
    "start:docker": "yarn prepare-sample-tar && cat sample-package/package.tar.gz | docker run -i --name pre-runner -v vol-$(git rev-parse --short HEAD):/package scramjet/pre-runner",
    "test": "echo \"Error: no test specified\"",
    "prepare-sample-tar": "mkdir -p sample-package && tar -zcvf ./sample-package/package.tar.gz -C ../samples/example .",
    "clean:docker": "docker volume rm $(git rev-parse --short HEAD); docker rm -f runner"
  },
  "author": "",
  "license": "AGPL-3.0",
  "publishConfig": {
    "registry": "https://repo.int.scp.ovh/repository/npm-scramjet/",
    "access": "restricted"
  }
}
