{
  "name": "@scramjet/types",
  "version": "0.2.0",
  "description": "types.",
  "main": "./src/runner.ts",
  "scripts": {
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "prepack": "node ../../scripts/publish.js",
    "build": "tsc -p tsconfig.json && npm run test:expose",
    "watch": "tsc -b --watch",
    "test:expose": "bash src/scripts/generate.sh",
    "test": "yarn run test:expose && npx tsc --noEmit"
  },
  "author": "Scramjet <open-source@scramjet.org>",
  "license": "AGPL-3.0",
  "devDependencies": {
    "@scramjet/symbols": "0.2.0",
    "@types/node": "^14.14.22",
    "scramjet": "^4.35.11",
    "typescript": "^4.1.4",
    "typedoc": "^0.20.23",
    "typedoc-plugin-markdown": "^3.4.5",
    "rimraf": "^3.0.2"
  }
}
