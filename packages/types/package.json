{
  "name": "@scramjet/types",
  "version": "0.7.0-pre.1",
  "description": "types.",
  "main": "./src/index.ts",
  "scripts": {
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "prepack": "LOCAL_PACKAGES=true node ../../scripts/publish.js",
    "build": "tsc -p tsconfig.json && npm run test:expose",
    "watch": "tsc -b --watch",
    "test:expose": "scripts/generate.js",
    "test": "yarn run test:expose && npx tsc -p ./tsconfig.test.json --noEmit"
  },
  "author": "Scramjet <open-source@scramjet.org>",
  "license": "AGPL-3.0",
  "dependencies": {
    "@scramjet/model": "^0.7.0-pre.1",
    "@scramjet/symbols": "^0.7.0-pre.1"
  },
  "devDependencies": {
    "@types/node": "14.14.33",
    "rimraf": "^3.0.2",
    "scramjet": "4.35.15",
    "typedoc": "0.20.28",
    "typedoc-plugin-markdown": "3.4.5",
    "typescript": "4.1.4"
  },
  "publishConfig": {
    "registry": "https://repo.int.scp.ovh/repository/npm-scramjet/",
    "access": "restricted"
  }
}
