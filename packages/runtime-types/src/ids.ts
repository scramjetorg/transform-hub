/**
 * Basic identifier types for runtime entities.
 *
 * Simplified structural copies migrated from the old types package.
 */

export type InstanceId = string;
export type SequenceId = string;
export type HostId = string;
